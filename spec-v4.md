# 《问道》四期设计规格 (spec-v4)

> 在 v3 实现态（炼虚圆满 + 师徒/道侣/共修 + 拍卖行 + 心魔劫 + 本命法宝）基础上的四期设计。
> v4 核心命题：**从挂机数值游戏，变成有决策、有过程、有意外的游戏**。
> 范围：境界上限→合体圆满；新增灵兽、奇遇、征途（Roguelike秘境）、镇妖塔、洞府/灵田、神通对决层。
> 本文件只记录需要实现、迁移、验收或明确排除的技术规格。

文档状态：**设计草案，已与用户商定范围（2026-07-14）；专家审核后二次修订**。
数值标注"初版可调"者均需配合 `tools/balance_sim.py` 与上线数据继续调参。

---

## 0. v4 范围与边界

| # | 决定 | 取值 |
|---|---|---|
| 1 | 版本目标 | **提升可玩性三维**：决策密度（奇遇/征途/神通组合）、过程体验（征途中间决策/奇遇分支）、目标多样性（镇妖塔长线/洞府经营/技能 build）；同步推进合体境界与灵兽轴 |
| 2 | 境界上限 | 从炼虚圆满提升到**合体圆满**；大乘及以上留五期 |
| 3 | 灵兽 | 战斗宠物轴，支援单位参战；元婴期起；非绑定可交易 |
| 4 | 奇遇系统 | 历练/秘境/征途触发文字选择事件；允许负面结果（受伤/损失/debuff）；复用并扩展现有 `config/events.py` 的 ENCOUNTERS 结构 |
| 5 | 征途模式 | Roguelike 分叉秘境，与现有秘境**并存**、零改动现有逻辑；每层选门，run 内临时增益；仅靠征途令控频，**无每日次数上限** |
| 6 | 镇妖塔 | 永久攀层进度 + 首通奖励 + 周群内排行（本周新推进层数）；所有养成在此兑现 |
| 7 | 洞府/灵田 | 洞府升级解锁格数；灵田种植→定时成熟→收获材料；惰性结算 |
| 8 | 神通对决层 | 合体期解锁第 4 上阵槽 + 新功法池；不同场景最优解分化；学习与装备拆两张表 |
| 9 | 跨群玩法 | 全服榜单/跨群拍卖/跨群 Boss **全部不做**，继续押后 |
| 10 | 高风险 PvP | 生死斗继续押后，本期不做 |
| 11 | 变现 | 沿用历版决定：**不引入付费** |
| 12 | 玩家存档 | 不重置、不回档；炼虚圆满玩家直接接入新内容 |

规格原则：

- **二值引擎是朋友**：战斗近二值使临时 buff 成为字面意义的"钥匙"——征途/镇妖塔/奇遇战斗分支都围绕此特性设计。
- **灵兽不进 clamp**：战斗单位形态参战，用独立贡献占比预算（§4.6）约束。
- **奇遇是内容底座**：`config/events.py` 同时供历练触发、征途"遇"门、链式奇遇使用；后续追加内容只需填表。
- **周事件总量**：征途靠令牌控频而非每日次数，镇妖塔只奖首通、每日安慰奖极轻量，灵田支持一键收获/补种，不新增强制日活动项；每里程碑上线前审计"新增每日主动操作目标不超过 5 分钟"。

---

## 1. 当前实现依赖

v3 落地的这些能力，四期必须复用而不是另起炉灶：

| 现有能力 | 四期复用方式 |
|---|---|
| `config.realms` 境界配置（含炼虚 realm 5） | 追加合体 realm 6 |
| 每境界三档历练图 + 低阶折叠 + 扫荡 | 给合体补易/中/难三张图；元婴+地图追加灵兽出没与奇遇触发配置 |
| 炼虚丹管线（残方 ×4 合成 + `big_fail_streak` 失败累计保底） | 合体丹完整复用；`big_fail_streak` 读取范围扩展至化神→炼虚、炼虚→合体两档 |
| 交互式天劫 `tribulation_sessions`（含心魔劫选项） | 炼虚→合体改为"天人劫"三段；直面心魔选项通用实现自动覆盖新劫 |
| 溢出分流 `overflow_split` 按 `len(REALM_NAMES)` 自动上移 | 追加 realm 6 即生效；降档宽限期机制（`game_flags`）复用，M0 重写宽限截止 |
| 世界 Boss 分档 + 按群内人数缩放 | 新增合体 Boss 档 `heti` |
| 拍卖行（英式+buyout、escrow 实扣、防狙击、实例托管 `status`、审计） | 新增 kind `'beast'`；灵兽上拍复用全链路 |
| `callback_tokens` + `db.transaction()` | 捕捉/放生/守猎/征途结算/洞府升级/镇妖塔挑战全部走一次性 token + 原子事务 |
| `game_events` 领域事件 | 合体突破、稀有灵兽、天价成交、链式奇遇大机缘、镇妖塔百层首通进入播报 |
| 绑定库存 `inventory(bound)` | 守猎/灵田普通产出、进阶材料默认 `bound=1`；灵田精品产出用独立 item_key、`bound=0` |
| buff 合算 clamp（ATTACK/SURVIVAL 0.25、SECLUSION 0.60） | 奇遇/洞府轻数值加成接入 clamp；灵兽不进 clamp |
| `config.events` + ENCOUNTERS | **迁移并扩展**现有遭遇表，不平行新建；新奇遇事件以相同结构追加 |
| `tools/balance_sim.py` 多档 profile | 新增炼虚满 buff 档、合体各档、有兽档矩阵 |
| 通知任务（ready-action notify，每 1 分钟） | 守猎到期、灵田成熟、链式奇遇到期全部复用 |
| `character_skills` | **M6 迁移**：拆为 `character_learned_skills` + `character_equipped_skills`（§9.2）；迁移前新功法以现有结构暂存，M6 统一迁移 |

当前实现约束：

- 战斗引擎近二值：临时 buff 是**门槛平移器**，不是平滑胜率增量——这是优势不是约束。
- 经济反套利红线：守猎/灵田普通产出全绑定；奇遇大机缘产物设绑定；征途奖励不含直接灵石喷发；灵兽交易只转移灵石不产生灵石。
- 高境界裸装遗留课题：合体 M0 验收 profile = 合体初期锚点 + 现役炼虚装备/功法。

### 1.1 v3 收口项（M0 前置）

1. **v3 全里程碑验收断言在 main 全绿**：`python -m pytest` + `python -m tools.balance_sim` 面板人工过一遍，确认炼虚满 buff 档（满淬炼 + 道途拉满 + 飞升被动满 + 本命满级）已在 sim 中。
2. **飞升点产销再审计**：合体溢出分流会加大飞升点供给，天门叩问节奏在合体产出下不塌方，用 balance_sim 复核。

---

## 2. 四期核心循环

```
  炼虚圆满
      ↓
  [合体突破] 合体丹 + 天人劫（含直面心魔）
      ↓
  [合体期 PvE] 周天星海 / 阴阳鸿沟 / 寂灭深渊 / 九天玄宫 / 噬星古鲲
      ↓                    ↘                   ↘                  ↘
  [溢出分流上移]         [灵兽轴]           [奇遇系统]          [洞府/灵田]
  道行/飞升点              捕捉→养成→参战       历练/征途触发         种植→成熟→材料
  (现有 sink 上移)          守猎→材料 sink        链式奇遇/大机缘        洞府升级解锁
      ↓                    ↓                   ↓                  ↓
  [征途模式]  ←——————奇遇"遇"门事件共用——————→  [镇妖塔]
  每层分叉选门               同事件表              永久层数 / 周新推进榜
  run内临时增益（道器碎片）                         全部养成在此兑现
      ↓
  [神通对决层]  第4槽解锁 / 合体新功法 / 场景分化 build
      ↓
  继续循环：宗门战 / PvP 天梯 / 师徒 / 道侣 / 本命法宝
```

---

## 3. 境界上限扩展：合体期

### 3.1 新境界结构

| 大境界 | 小阶 | 主要解锁 |
|---|---|---|
| 炼虚期 | 初 / 中 / 后 / 圆满 | 当前 v3 上限 |
| **合体期** | 初 / 中 / 后 / 圆满 | 合体地图、九天玄宫、合体 Boss、合体装备、第 4 上阵槽（§9.2）、溢出分流上移 |
| 大乘期+ | 待定 | 五期 |

配置改动（均为初版可调）：

- `REALM_NAMES` 追加 `合体期`；`REALM_STAGES[6] = ["初期", "中期", "后期", "圆满"]`。
- `STAMINA_CAP[6] = 320`；`STAMINA_REGEN_SECONDS[6] = 121`（回满约 10.8 小时）。
- `STAMINA_BUY_BASE[6] = 8000`，当日首买 20 精力约 400 灵石/精力。
- `SECLUSION_STAGE_HOURS[6] = 960`（初版；配合合体图修为把普通活跃档推进压到 5~6 周）。
- `_REALM_BASE_COST[6] = 35_000_000`，`_STAGE_MULT` 沿用 1.20。
- `BIG_BREAKTHROUGH[6] = {"pill": "合体丹", "base_rate": 0.40, "tribulation": True}`。

### 3.2 合体属性锚点

当前炼虚圆满基础锚点 `hp=170000 / mp=13500 / atk=12000 / df=8800 / spd=2300 / crit=700`。
沿用现有跨境界倍率曲线（初阶≈上一境圆满×1.5，圆满≈本境初阶×2.2）：

| 境界 | 气血 | 法力 | 攻击 | 防御 | 身法 | 暴击 |
|---|---:|---:|---:|---:|---:|---:|
| 合体初期 | 255000 | 20000 | 18000 | 13200 | 3400 | 1050 |
| 合体圆满 | 560000 | 44000 | 40000 | 29000 | 7500 | 2300 |

平衡目标（全部按"可刷 / 门槛"口径；**验收档定义见 §13**）：

- 炼虚圆满**满 buff 档**可刷合体易图普通小怪。
- **过渡档**：炼虚圆满满 buff 档可低效磨合体中图普通小怪（收益明显差于易图但 > 0）。
- 合体中/难图 Boss 与多遭遇连战压住炼虚圆满满 buff 档（挑战成功率 < 5%）。
- 合体初/中/后期分别稳定解锁易/中/难图。
- **M2 灵兽上线后，元婴及以上全部 PvE 门槛改按"有兽档"复检**（§10.1）。

**合体期推进时长验收目标**：

- 普通活跃档（根骨 60、有效闭关 18h/日、一天一管精力 + 全日活）：合体初期→圆满 **5~6 周**。
- 高活跃档（根骨 70、有效闭关 22h/日、早晚两管 + 全日活）目标 **3~4 周**。
- 加上首破期望 4~6 周（§3.4），整体版本周期约 **9~12 周**。

装备验收口径：M0 = 合体初期锚点 + 现役炼虚装备/功法；M1 合体装备上线后新增合体 GEARED 档并回归重调。

### 3.3 炼虚→合体突破

| 目标境界 | 丹药 | 基础成功率 | 额外试炼 |
|---|---|---:|---|
| 合体期 | 合体丹 | 40% | 天人劫 |

规则：

- 触发条件：炼虚圆满 + 修为达到突破成本 + 合体丹。
- 失败轻惩罚：损失当前修为 30%，不跌境。
- **失败累计保底**：复用 `characters.big_fail_streak`，读取范围扩展为化神→炼虚、炼虚→合体两档（+10%/次，上限 95%，成功清零）。期望突破次数 ≈ 2.1 次。
- 天人劫复用 `tribulation_sessions` 三段状态机，文案换天人合一/神魂交融主题；**直面心魔选项自动覆盖新劫**。

### 3.4 合体丹来源

完整复用 v3 首破权重原则：

- 炼虚难图 `混沌古狱` 极低概率掉 `合体丹残方` 或 `合体丹`。
- 秘境 `虚空神殿` 深层掉丹方/材料；M1 后 `九天玄宫` 追加来源。
- 炼虚/合体世界 Boss 前列奖励掉材料，参与奖不直接掉完整丹。
- 合体易图低概率掉丹材（仅已突破玩家加速补充）。
- NPC 商店不直售；坊市/拍卖行均不可上架合体丹。
- **残方合成保底**：`合体丹残方` ×4 合成 1 枚合体丹（绑定）。
- **签到保底**：沿用炼虚丹签到保底模型（c2be7dd），炼虚圆满玩家累计签到发残方。
- **征途深层**（§6）：征途通关高层有低概率产出合体丹材料（M4 起）。

**验收口径**：首枚合体丹期望路径 100% 在炼虚可及内容；正常日程期望 **2~4 周**得首枚；期望"进合体"周期 **4~6 周**。

**M0 预置线索**：M0 同步在炼虚难图/虚空神殿低概率掉落合体装备图纸残页（展示"待解锁"），并预埋 M2 奇遇文案（历练偶遇灵兽踪迹的氛围播报，无功能）。

### 3.5 合体历练地图

| 地图 | 难度 | 精力 | 时长 | 独占掉落 |
|---|---|---:|---|---|
| 周天星海 | 易 | 24 | 18~22 分钟 | 星海尘晶 |
| 阴阳鸿沟 | 中 | 28 | 22~26 分钟 | 阴阳玄玉 |
| 寂灭深渊 | 难 | 32 | 26~30 分钟 | 寂灭道痕 / 合体丹材料 |

独占材料用于：合体丹、合体装备、道途宗师进度、飞升点材料、灵兽喂养、洞府升级、本命法宝喂养。
地图灵石/精力产出比低于首买成本（400）至少 25%；修为收益明显高于炼虚图。

### 3.6 合体秘境：九天玄宫

| 配置 | 初版取值 |
|---|---|
| 解锁 | 合体初期 |
| 层数 | 7 |
| 精力 | 80 |
| 入场费 | 8000 灵石 |
| 每日次数 | 2 |
| 主要产出 | 合体丹材料、合体法宝、道途/飞升材料、灵兽材料、征途令（M4）|

合体初期推进 40%~70% 层数；合体圆满稳定通关。

### 3.7 合体世界 Boss 档

新增 Boss 档 `heti`：

| Boss | 面向境界 | 持续 | 精力 | 奖励定位 |
|---|---|---:|---:|---|
| 噬星古鲲 | 合体 | 2 小时 | 20 | 合体材料、道途材料、飞升点材料、灵兽材料 |

沿用同群独立、按群内人数缩放、参与奖+贡献奖+前列奖励的现行模型。

### 3.8 长线系统上移

- **溢出分流**：机制随顶点自动上移——合体圆满：顶点档（×8% 道行 + 飞升点，周上限沿用）；炼虚圆满未突破：次顶点档（×3%、无飞升点）。
- **降档宽限期**：M0 首启迁移把 `game_flags.overflow_demote_grace_until` **重写为部署时刻 + 28 天**；缺公告文案 M0 不得上线。
- **道途/飞升/天门**：合体内容给道行；宗师进度追加合体材料配方；天门费用曲线不动；合体溢出加大飞升点供给，按 §1.1 复核。
- **本命法宝**：等级上限维持 10 不变；灵兽是本期材料 sink 主承接。

---

## 4. 灵兽系统：捕捉、养成与参战

### 4.1 定位

灵兽是与境界正交的**战斗宠物轴**：支援单位形态参战，捕捉获取、喂养成长、兽魂进阶，本体**非绑定可交易**（拍卖行），并可派遣守猎产出喂养材料。

灵兽与奇遇（§5）和征途（§6）天然融合：稀有灵兽的捕捉机会可以出现在奇遇大机缘和征途"遇"门里。

### 4.2 捕捉

**触发**：元婴期起，在配置了灵兽出没的历练地图打赢一场遭遇战后，概率触发"灵兽现身"（初版 12%，可调）。玩家在 10 分钟内（一次性 token）选择是否消耗 1 根 `缚灵索` 尝试收服；超时则灵兽遁走，不计保底。

**缚灵索**：炼器可制（现有材料 + 少量新图材料），不绑定、可坊市交易——是捕捉行为的主要灵石 sink。

**成功率与保底**：

| 稀有度 | 基础捕捉率 | 出没范围 |
|---|---:|---|
| 常见 | 35% | 各档地图均有 |
| 稀有 | 12% | 中/难图、秘境深层、征途精英门 |

- **失败递增率保底**：每次捕捉失败 +8%，捕捉到任意灵兽即清零；总率上限 90%。落点：`characters.beast_capture_streak`。
- **奇遇/征途渠道**：奇遇大机缘和征途"遇"门可触发稀有灵兽出没（保底计入同一 streak）。
- 兽栏已满（3 格）不触发现身，提示先放生/出售。

**灵兽档位**：按出没地图境界分档（元婴/化神/炼虚/合体），强度锚定自身档位——主人升境后旧兽相对变弱，是经济跨版本持续的结构性设计。

### 4.3 种族与资质

**原型**（对应现有技能类型，不新增战斗机制）：

| 原型 | 灵技行为 |
|---|---|
| 爆发 | 高系数单次伤害，cd 2 |
| 灼烧 | 挂 dot，cd 4 |
| 疗愈 | 回复主人气血（≈max_hp 2~3%），cd 3 |
| 定身 | 定住敌方一回合，cd 5 |

**初版种族表**（4 档 × 4 原型 = 16 常见种族 + 每档 1 稀有）：

| 档位 | 爆发 | 灼烧 | 疗愈 | 定身 | 稀有 |
|---|---|---|---|---|---|
| 元婴档 | 裂风隼 | 赤尾火狐 | 灵蕴白鹿 | 镇岳石犀 | 雷瞳貔貅 |
| 化神档 | 赤焰狻猊 | 幽冥火蟒 | 月华仙狐 | 玄冰玳龟 | 吞霄天狼 |
| 炼虚档 | 掣电天隼 | 蚀骨魔蛛 | 青鸾 | 憾山魁牛 | 太虚孔雀 |
| 合体档 | 噬金猰貐 | 烛阴幼蟒 | 九色神鹿 | 混沌玄武 | 星渊烛龙 |

**资质**：捕捉时一次性随机（0.85~1.15，四项独立 roll，存 JSON），终身不变。稀有种族 roll 区间上移（0.95~1.25）。

### 4.4 养成：喂养与进阶

**等级（喂养）**：消耗 `兽粮`（炼器可制）提升等级；每级属性 +3%；等级上限随主人境界：元婴 20 / 化神 30 / 炼虚 40 / 合体 50；喂养进度随实例交易保留。

**星级（进阶）**：0~3 星；每星资质 +5%（乘算），消耗同档 `兽魂` ×（4/8/16）；`兽魂` 来源：放生返还（常见 1/稀有 4）、守猎稀有产出、合体难图小概率；**兽魂绑定**不可交易。

放生/多余捕捉 → 兽魂 → 主力兽升星，形成"捕捉永远有价值"的回收闭环。

### 4.5 参战机制（战斗引擎扩展）

灵兽以**支援单位**形态进入 `services/combat.simulate`：

- `simulate()` 新增可选参数 `a_support` / `d_support`（默认 None，**不传时行为与现行完全一致**）。
- 支援单位每回合在**主人行动之后**出手一次（主人被定身则灵兽也蛰伏该回合）。
- 支援单位**不可被选中、不持有战斗内血条**——把引擎改动面压到最小。
- 支援单位有自己的 mp/冷却，走同一 `_choose_skill` / `_hit` 管线；伤害受 `CombatRules` 的 damage_growth/治疗衰减等禁制同等约束。
- 战斗日志出手行带 🐾 前缀。
- 出战兽由 `characters.active_beast_id` 指定；守猎中/上拍托管中的灵兽不可出战。

**战斗属性推导**：

```
兽属性 = 种族基础（按档位锚定）× (1 + 0.03 × 等级) × 资质 × (1 + 0.05 × 星级)
```

### 4.6 贡献占比预算（平衡红线）

- **输出预算**：同档满级 3 星高资质兽的期望 DPS ≤ 同档 GEARED 玩家期望 DPS 的 **15%**；常见 0 星初捕兽约 6~8%。
- **治疗预算**：疗愈原型每回合期望治疗 ≤ 主人 max_hp 的 3%。
- **控制预算**：定身原型 cd ≥ 5，与玩家定身符共用单一 stunned 标记；断言不存在"玩家+兽轮流定身锁死"（PvP 固定 seed 穷举）。
- 超预算调参入口：种族基础比例、灵技系数、等级成长率——全部集中在 `config/beasts.py`。

### 4.7 守猎（非战斗副产出）

- 出战外的兽栏灵兽可派遣守猎：**4 / 8 / 12 小时**三档，后台进行，**不占前台活动窗口**。
- 产出：`兽粮` 材料为主，小概率同档图普通材料、极小概率 `兽魂`；**全部绑定**，不含灵石。
- 产出量随时长次线性（12h ≈ 4h 的 2.2 倍）；每兽每日最多 2 次派遣。
- 守猎中的兽不可出战/上拍/放生；到期不自动收，**惰性结算** + ready-action 通知任务。

### 4.8 交易（拍卖行接入）

- 灵兽只走拍卖行；坊市不收。
- 拍卖 kind 新增 `'beast'`，`instance_id` 指向 `spirit_beasts.id`；上拍托管复用 `status` 状态机。
- 成交过户 `owner_id`，等级/星级/资质随实例保留；流拍恢复 `status='idle'`。
- escrow 实扣、防狙击、buyout、挂拍费 1%、成交税 10%、审计口径全部复用 v3 拍卖行。
- 稀有种族成交、天价成交进 `game_events` 播报。

---

## 5. 奇遇系统

### 5.1 定位

奇遇是**可持续扩充的内容底座**，**迁移并扩展现有 `config/events.py` 的 ENCOUNTERS 结构**，不平行新建服务层。历练/征途/秘境中随机触发文字选择事件；修仙小说的核心爽点是机缘，奇遇把这个情绪补回来。

### 5.2 触发逻辑

| 触发时机 | 基础触发率 | 说明 |
|---|---:|---|
| 历练结算后 | 8% | 每次历练结束时（精力已花），独立骰子 |
| 秘境收功结算 | 5% | 常规秘境；征途有独立"遇"门（§6.3），不再额外骰子 |
| 征途"遇"门 | 100% | 玩家主动选了遇门才触发 |
| 签到 | 2% | 限低概率普通奇遇；不触发链式奇遇或大机缘 |

**触发即计冷却**：历练/秘境/签到触发奇遇后，**立即**计入 4 小时冷却（无论玩家是否在超时内响应）。征途"遇"门不受此冷却约束（主动进门）。

**超时处理**：弹出 token 超时（15 分钟），自动按**保守选项**结算。保守选项的设计约束：

- **不消耗任何资源**（不扣灵石/精力/材料）。
- **不获得任何实质奖励**（可以有"缘分擦肩而过"的纯文案）。
- 链式奇遇超时按"缘分已散"结算（给极小安慰文案，无奖励），进度清除。

这样玩家无法通过反复放弃奇遇来"筛选"触发时机——触发了就算数，选或不选（超时=保守选）。

进行中的链式奇遇**不叠加**触发新链式奇遇，可叠加普通奇遇。

### 5.3 事件结构

事件定义迁移扩展现有 `config/events.py` 的 ENCOUNTERS 格式：

```python
{
    "key": "abandoned_cave",
    "name": "荒废洞府",
    "rarity": "common",          # common / rare / chain / grand
    "realm_min": 0,
    "desc": "前方隐约传来阵法波动……",
    "options": [
        {
            "label": "循波入内",
            "cost": {"stamina": 5},        # 可选代价；保守选项 cost 必须为 {}
            "prob_success": 0.7,            # 省略则必然成功
            "success_effects": {"item": "灵草", "qty_range": [2, 5]},
            "failure_effects": {"hp_pct": -0.2, "flavor": "阵法反噬，受了些伤。"},
            "success_flavor": "阵法残破，得了些灵草。",
            "chain_next": None,             # 成功后的链式下一节点 key（此选项触发）
        },
        {
            "label": "谨慎绕行",            # 保守选项
            "cost": {},
            "success_effects": {},
            "success_flavor": "你避开了这处废墟，继续赶路。",
            "chain_next": None,
        },
    ],
}
```

`chain_next` 放在**选项级**（不同选择可走不同后续链），不在事件级。

**稀有度等级**：

| 稀有度 | 触发权重 | 典型效果 |
|---|---:|---|
| `common` | 70% | 小额材料/灵石/道行/debuff |
| `rare` | 25% | 功法残页线索、大量材料、稀有灵兽出没引导、一次性强力 buff |
| `chain` | 4% | 链式奇遇头节点，跨 1~3 天展开，终端有 `rare` 级奖励 |
| `grand` | 1% | 大机缘：丹方完整版、功法实例、稀有灵兽直接出没（走捕捉流程） |

### 5.4 选项代价与负面结果

**正面代价**（支付即触发，无失败风险）：消耗灵石/精力（3~8）/绑定材料。

**赌性选项**（有 `prob_success < 1`）的失败惩罚类型：
- **受伤**：当前 HP ×(1 - pct)，pct ∈ [0.15, 0.30]，不低于 1，不致死。
- **损失灵石**：随机丢失相当于半日活量级。
- **中毒 debuff**：下次历练首回合 HP -15%（存 `characters.debuff_until`）。
- **耗损法力**：当前 MP -30%。
- **触发战斗**：与事件怪物一战，败则受伤；胜则额外奖励（`simulate()`，出战兽参与）。

负面结果永远不能损失绑定物、灵兽、稀有实例；最坏情况可恢复。

### 5.5 链式奇遇

- 头节点触发后，在 `adventure_sessions` 表记录进度；`ready_at` 指定下一步可触发时间。
- ready-action 通知任务到 `ready_at` 后推送，玩家主动触发下一步。
- 链式最多 3 步；超时（48h）按"缘分已散"结算（无奖励，清除进度）。
- 玩家可"放弃"（保守选项），返还已支付代价的 30%。
- **M3 初版事件包**：普通 30 / 稀有 12 / 链式 4 / 大机缘 3（总计 ≥ 49），每季度追加 ≥ 10 个。

### 5.6 数据层

```sql
adventure_sessions(
  id         INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id    INTEGER NOT NULL,
  event_key  TEXT NOT NULL,
  step       INTEGER NOT NULL,         -- 0=头节点, 1..N=后续步
  ready_at   INTEGER NOT NULL,         -- 下一步可触发时间戳;当前步未完成时=创建时刻
  expire_at  INTEGER NOT NULL,         -- 超时截止(ready_at + 48h)
  status     TEXT NOT NULL DEFAULT 'pending',  -- 'pending'|'done'|'expired'
  notified_at INTEGER,                 -- 通知任务已推送时间戳;NULL=未推送
  state      TEXT                      -- 跨步共享状态 JSON(可选)
);
CREATE UNIQUE INDEX idx_adv_active ON adventure_sessions(user_id)
  WHERE status='pending';             -- 一个玩家同时只有一条进行中的链式进度

-- characters 追加 (_ensure_column) --
-- debuff_until INTEGER         中毒 debuff 到期时间戳;NULL=无
-- last_event_at INTEGER        上次历练/秘境触发奇遇时间戳;4h 冷却
```

---

## 6. 征途模式（Roguelike 秘境）

### 6.1 定位

征途是**与现有秘境并存的新模式**，现有秘境（灵溪洞、玄冥矿窟、青云秘境、虚空神殿、九天玄宫）零改动。核心命题：**每层出 2~3 扇门，每扇门是一次真实决策**，5~8 次决策之后才看结果。

与现有秘境对比：

| 维度 | 现有秘境 | 征途模式 |
|---|---|---|
| 决策次数 | 0（点进去看结算） | 5~8 次（每层选门） |
| 奖励结构 | 固定按层数 | Run 内累积 + "见好就收"张力 |
| 与奇遇关系 | 偶尔触发奇遇 | 遇门 = 奇遇事件，直接复用事件表 |
| 灵兽参与 | 参战 | 参战（定身兽让一扇精英门从打不过→打得过）|

### 6.2 入口与消耗

- 解锁：**金丹期起**（决策感比灵兽更早给到玩家）。
- 消耗 `征途令`（来源：九天玄宫掉落 / 世界 Boss 前列 / 每周活跃签到 / 镇妖塔首通奖励）。
- **无每日次数上限**：征途令是唯一限频机制；令牌稀缺度控制进入频率。
- 征途中途可"见好就收"：任意层后选择退出，带走已积累奖励的 100%。

### 6.3 门类型

每层随机生成 2~3 扇门（配置驱动，权重按征途档位调整）：

| 门类型 | 权重（初版） | 内容 |
|---|---:|---|
| 战 | 35% | 普通遭遇战，胜利得基础奖励 + 推进，败则受伤至 30% HP 继续 |
| 精英 | 15% | 强化怪，难度高于当前档位普通怪一级；首通奖励丰厚（合体丹材料/稀有兽魂）|
| 宝 | 25% | 无战斗，直接得道器碎片（run 内临时增益，§6.4）或材料 |
| 遇 | 15% | 触发奇遇事件（从 §5 事件表抽取，realm 匹配当前档位），100% 触发 |
| 休整 | 5% | 无战斗，恢复 HP 至 60%（缓解"战败继续"的无尽劣势）|
| 歧路 | 5% | 无战无宝，改变下一层的门类型分布（如高风险高回报路线）|

玩家永远有至少 2 扇门可选，不会被强制走单一路线。"见好就收"按钮在每层选门前均可点击。

### 6.4 Run 内临时增益（道器碎片）

临时增益**只活在本次 Run 里**，收功即消失——不碰 clamp，不碰永久面板，平衡隔离天然。

| 道器碎片 | 效果 | 对二值引擎的含义 |
|---|---|---|
| 破障符 | 本 Run 全程 atk +20% | 把"打不过"的精英门变成"打得过" |
| 护体光幕 | 本 Run 全程 hp +15% | 多遭遇连战增加容错 |
| 速攻令 | 本 Run 全程 spd +25% | 先手优势，对定身/爆发类有倍增效果 |
| 汲灵珠 | 技能 MP 消耗 -30% | 大 MP 技能可以多轮使用 |
| 护法符 | 第一次战败时 hp 恢复至 50% 再战一次 | 续命，可贪深层 |

- 宝门、精英门胜利后可获得道器碎片；每次 Run 最多持有 3 个。
- 重复获得同种碎片时自动转化为小额材料（不浪费）。
- 碎片选择在宝门/精英门收益时弹出按钮（2~3 选 1）——这是 Run 内最重要的决策时刻。

### 6.5 战败与"贪深"惩罚

- 被打败时：**HP 恢复至 30%** 后可继续（不是→1，30% 有继续作战能力）。
- 护法符触发时：**HP 恢复至 50%** 后再战一次，之后护法符消耗（`consumed_lifesaver=1`）。
- 累计战败 3 次则**强制退出**，奖励 ×70%（惩罚贪深）。
- 主动"见好就收"退出：奖励 ×100%。
- 休整门可将 HP 恢复至 60%，是打与退之间的重要缓冲。

### 6.6 征途档位

| 档位 | 解锁境界 | 层数 | 主要奖励 |
|---|---|---:|---|
| 洞天层 | 金丹 | 6 | 金丹/元婴材料、低档兽魂 |
| 秘境层 | 元婴 | 8 | 元婴/化神材料、中档兽魂、功法残页 |
| 禁地层 | 炼虚 | 10 | 炼虚/合体材料、高档兽魂、合体丹材料 |
| 天外层 | 合体 | 12 | 合体材料富集、星渊兽魂、稀有丹方线索 |

玩家进入自己境界解锁的最高档；低档产出上限低于当档日常，进去没有意义（自然劝退刷低档）。

### 6.7 数据层

```sql
expedition_runs(
  id              INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id         INTEGER NOT NULL,
  tier            TEXT NOT NULL,        -- 'dongtian'|'secret'|'forbidden'|'void'
  floor           INTEGER NOT NULL DEFAULT 0,
  hp_snapshot     INTEGER NOT NULL,     -- 出发时 HP 快照（同现有秘境逻辑）
  mp_snapshot     INTEGER NOT NULL,
  current_hp      INTEGER NOT NULL,     -- 当前层剩余 HP（战败恢复后更新）
  current_mp      INTEGER NOT NULL,     -- 当前层剩余 MP
  run_seed        INTEGER NOT NULL,     -- 本次 run 随机种子（确定性）
  current_doors   TEXT,                 -- 当前层已生成的门 JSON（防刷新）
  floor_settled   INTEGER NOT NULL DEFAULT 0, -- 当前层是否已结算
  buffs           TEXT,                 -- run 内临时增益 JSON 列表
  consumed_lifesaver INTEGER NOT NULL DEFAULT 0, -- 护法符是否已消耗
  fail_count      INTEGER NOT NULL DEFAULT 0,
  rewards         TEXT NOT NULL DEFAULT '{}',
  started_at      INTEGER NOT NULL,
  status          TEXT NOT NULL DEFAULT 'active' -- 'active'|'done'
);
CREATE UNIQUE INDEX idx_exp_active ON expedition_runs(user_id) WHERE status='active';
-- 一个玩家同时只有一次进行中的征途
```

现有 `services/explore.py`、`handlers/explore.py`、callback 前缀 `ex:` 全部保留给历练，**不复用、不覆盖**。
征途新增：`services/expedition.py`、`handlers/expedition.py`、callback 前缀 `exp:`、命令 `/venture`（中文名：征途）。

---

## 7. 镇妖塔

### 7.1 定位

镇妖塔是**全游戏的度量衣**：所有养成投入（境界/装备/道途/灵兽/神通）最终都在"我能打到第几层"上兑现。它回答"我变强了，所以呢？"。

### 7.2 规则

- 解锁：**炼气期即可进入**（最低门槛，所有玩家有塔可打）。
- 每层一个固定怪物配置（config 化），难度严格递增。
- 层数进度**永久记录**（`characters.tower_floor`），只进不退。
- **首通奖励**：在同一事务里比较挑战前后的 `tower_floor`，新高层时发放对应首通奖励，**不使用全局 `game_flags`**（避免跨玩家污染）。首通奖励一次性，之后该层只有挑战小奖。
- 每次挑战消耗 5 精力（轻量）；带出战兽参战。
- 连续失败同一层 3 次后，该层当日锁定（防无限嗑）。

### 7.3 层数设计

| 层段 | 对应门槛 | 首通奖励重点 |
|---|---|---|
| 1~20 层 | 炼气/筑基可达 | 灵石、常见材料、基础功法残页 |
| 21~40 层 | 金丹/元婴门槛 | 中阶材料、稀有功法残页、首枚缚灵索 |
| 41~60 层 | 化神/炼虚门槛 | 炼虚丹材料、高档兽魂、道途宗师进度 |
| 61~80 层 | 合体门槛 | 合体丹材料、稀有丹方线索、神通残页 |
| 81~100 层 | 合体圆满+满配+有兽 | 大机缘级奖励（首通群播报）|
| 100 层+ | 未来版本扩展 | 五期再开 |

层数验收口径：该层目标档位满 buff 有兽档"刚好可刷"；无兽档"可努力打过"（防无兽玩家被拒）。

### 7.4 周群内排行

- **周榜排名依据：本周新推进层数**（`tower_weekly_progress`，周一 0:00 重置为 0）。
- 永久最高层（`characters.tower_floor`）只做常驻展示，不参与排名——防止先玩家永久霸榜。
- 每周一 0:00 统计全群本周推进层数，排行榜发群；前 3 名获本周特殊奖励（绑定材料包）。
- 同群跨账号奖励：奖励发放绑定，不可转移，多群账号以各群独立计算（群内生态独立）。
- 并列时先达先排（本周内达到该推进数字的时间戳）。

### 7.5 数据层

```sql
-- characters 追加 (_ensure_column) --
-- tower_floor INTEGER NOT NULL DEFAULT 0   永久最高层
-- tower_week_progress INTEGER NOT NULL DEFAULT 0  本周新推进层数(周一重置)

tower_daily(
  user_id    INTEGER NOT NULL,
  day        TEXT NOT NULL,               -- 'YYYY-MM-DD'(Asia/Shanghai)
  fail_count INTEGER NOT NULL DEFAULT 0,  -- 当日连续失败计数（通过后清零）
  PRIMARY KEY(user_id, day)
);
```

周一重置 `tower_week_progress=0` 由 APScheduler 现有周任务承载（复用 PvP weekly settle 的触发时机，周一 0:00 新增一个 job）。

---

## 8. 洞府与灵田

### 8.1 定位

洞府/灵田提供**管理循环**：日常登录不只是"点收功然后等"，还有灵田收成要处理。惰性结算 + 通知任务保证不变成"必须准时签到"的负担。灵田支持**一键收获**（收全部已熟格）和**一键补种**（按上次种子配方补种），把操作压到最小。

洞府升级承接**合体期材料 sink**。

### 8.2 洞府

**等级**：0~5 级（0 级 = 未建立，任意境界均可激活）。

| 洞府等级 | 解锁功能 | 升级材料（初版可调）|
|---|---|---|
| 0→1 | 激活洞府，灵田 2 格 | 灵石 5000 + 筑基阶材料 |
| 1→2 | 灵田 4 格 | 灵石 20000 + 金丹阶材料 |
| 2→3 | 灵田 6 格 + 守猎每日限次 +1（3 次）| 灵石 80000 + 元婴阶材料 |
| 3→4 | 灵田 8 格 + 征途令每周产出 +1 | 灵石 300000 + 化神阶材料 |
| 4→5 | 灵田 10 格 + 每周签到奖励包升级 | 灵石 1000000 + 合体阶材料 |

洞府升级走 `db.transaction()`，消耗库存并校验等级上限；同时升级锁防并发。

### 8.3 灵田种植

**绑定规则**：普通产出（未施灵）`bound=1`；**精品产出（施灵后）用独立 item_key**（如 `spirit_herb_premium`），`bound=0`，可进坊市/拍卖行——当前 inventory 表无品质字段，精品独立 item_key 是唯一兼容方案。

**种子品类**（初版 6 种，配置化）：

| 种子 | 生长周期 | 普通产出 | 精品 item_key | 精品来源 |
|---|---:|---|---|---|
| 灵草种 | 1 天 | 灵草 ×2~4（绑定）| — | 不支持施灵 |
| 灵芝孢 | 2 天 | 灵芝 ×1~2（绑定）| 精品灵芝 | 施灵后 ×1（不绑）|
| 元火草 | 2 天 | 元火草 ×1~2 + 赤火砂（绑定）| 精品元火草 | 施灵后 ×1（不绑）|
| 天玉莲 | 3 天 | 天玉莲 ×1（绑定）| 精品天玉莲 | 施灵后 ×1（不绑）|
| 兽粮草 | 1 天 | 兽粮原料 ×3~5（绑定）| — | 不支持施灵 |
| 丹母土 | 3 天 | 丹母土 ×1（绑定）| 精品丹母土 | 施灵后 ×1（不绑）|

**种植流程**：

1. `/estate` → 灵田面板，显示各格状态（空/种植中/已成熟）。
2. 选择空格 → 选种 → 消耗 1 枚对应种子种下，记录 `planted_at` 和 `mature_at`。
3. 成熟时不自动收获；**惰性结算**：下次查看灵田时落账，成熟后不腐坏（可以拖着收）。
4. **施灵**：成熟后收获前可选择消耗 1 枚 `灵液`（炼器可制），产出升级为精品 item_key（不绑定）。
5. **一键收获**：一次按钮收全部已熟格，减少操作次数。
6. **一键补种**：按上次该格的种子配方自动补种（需库存中有对应种子）。
7. ready-action 通知任务在成熟时推送。

**种子来源**：坊市/拍卖行流通（不绑定）；炼虚/合体地图中低概率产出高阶种子；部分链式奇遇终端奖励含稀有种子。

**经济护栏**：精品产出进坊市/拍卖行，价格纳入每周价格巡检；天玉莲/丹母土精品纳入拍卖行白名单；单格最大产出值 ≤ 当档历练 1 天收益 30%（balance_sim 口径），不成为最优刷图替代路线。

### 8.4 数据层

```sql
estate_plots(
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id     INTEGER NOT NULL,
  slot        INTEGER NOT NULL,           -- 0~9，随洞府等级解锁
  seed_key    TEXT,                       -- NULL = 空格
  planted_at  INTEGER,
  mature_at   INTEGER,
  is_mature   INTEGER NOT NULL DEFAULT 0,
  ling_used   INTEGER NOT NULL DEFAULT 0, -- 已施灵标记
  UNIQUE(user_id, slot)
);

-- characters 追加 (_ensure_column) --
-- estate_level INTEGER NOT NULL DEFAULT 0
```

---

## 9. 神通对决层

### 9.1 定位

神通对决层让玩家**有理由去定制自己的技能槽**，而不是全服同一套。通过不同内容场景的最优解不同实现：镇妖塔高层怪物特性、征途精英门类型、PvP 对手的功法偏好——让"我今天该带哪 4 个"成为值得思考的问题。

### 9.2 数据模型：拆两张表

现有 `character_skills` 以 `(user_id, slot)` 为主键，`slot=-2` 无法保存多个未装备神通。**M6 迁移至两张表**：

```sql
-- 已学会的所有功法（含未装备）
character_learned_skills(
  user_id   INTEGER NOT NULL,
  skill_key TEXT NOT NULL,
  learned_at INTEGER NOT NULL,
  PRIMARY KEY(user_id, skill_key)
);

-- 当前装备槽位（仅已装备的）
character_equipped_skills(
  user_id   INTEGER NOT NULL,
  slot      INTEGER NOT NULL,  -- -1=心法槽; 0..3=战斗槽
  skill_key TEXT NOT NULL,
  PRIMARY KEY(user_id, slot),
  FOREIGN KEY(user_id, skill_key) REFERENCES character_learned_skills
);
```

**迁移策略（M6 执行）**：

```sql
-- 迁移现有 character_skills 数据：
INSERT INTO character_learned_skills(user_id, skill_key, learned_at)
  SELECT DISTINCT user_id, skill_key, 0 FROM character_skills;
INSERT INTO character_equipped_skills(user_id, slot, skill_key)
  SELECT user_id, slot, skill_key FROM character_skills WHERE slot >= -1;
-- M6 上线后删除旧表
```

**战斗槽动态化**：`COMBAT_SLOTS` 不再是全局常量。服务层改为 `combat_slots_for(character)` 函数，合体初期+天人劫条件满足（`extra_skill_slots=1`）时返回 `range(4)`，否则返回 `range(3)`。合体前的角色行为与现行完全一致。

### 9.3 第 4 上阵槽位

- 解锁条件：`realm >= 6`（合体期）+ 完成一次天人劫；解锁状态存 `characters.extra_skill_slots`（整数，0 或 1），`_ensure_column` 迁移。
- 4 槽解锁后，`/skills` 界面和学习/装备逻辑按 `combat_slots_for(character)` 动态适配。

### 9.4 战斗 AI 基础条件（实现规格）

现有 `_choose_skill()` 按槽位顺序选第一个冷却和法力允许的技能。神通对决层的"场景分化"依赖以下最小条件扩展，否则玩家不是在设计流派而是在猜 AI 是否会乱放：

| 技能类型 | 释放条件（追加） |
|---|---|
| `heal` / `heal+` | 仅在主人当前 HP < max_hp × 70% 时释放；否则跳过选下一个 |
| `shield` / `shield+` | 无条件释放（同现在），但同回合内已有护盾则跳过 |
| `stun` | 目标已处于 `stunned` 状态时跳过（不重复定身浪费 MP）|
| `purify`（净化心光）| 仅在主人有 debuff 时优先释放，无 debuff 时正常按优先级轮转 |

**技能优先级 = 槽位顺序**：玩家通过调整槽位顺序来定义"主力技"，`_choose_skill` 按 slot 0→1→2→3 顺序选首个满足条件的技能。这是"构筑选择"的核心交互。

战报需展示技能跳过原因（如"（满血跳过）"），否则玩家无法理解 AI 行为来优化构筑。

### 9.5 新功法池

**合体期神通**（初版 8 个，按 `realm_min=6` 限制学习；以**神通残页**形态掉落，使用即学习，绑定）：

| 神通 | 类型 | 行为 | MP | CD | 来源 |
|---|---|---|---:|---:|---|
| 混沌斩 | burst | 高系数单攻（coef 2.8） | 50 | 3 | 镇妖塔 61 层首通 |
| 噬魂火 | dot | 双层灼烧叠加（coef 0.5×2） | 40 | 5 | 征途禁地层宝门 |
| 霸体诀 | shield+ | 护盾 + 下回合反伤 10% | 30 | 4 | 九天玄宫深层 |
| 天命一击 | burst | 暴击时 coef ×1.5 额外结算 | 35 | 3 | 噬星古鲲前列奖 |
| 玄冰禁锢 | stun+ | 定身且对方技能 CD+1 | 40 | 6 | 链式奇遇终端 |
| 净化心光 | heal+ | 回复主人 hp 20% + 解毒 | 45 | 4 | 合体 Boss 参与奖 |
| 太虚剑意 | normal+ | 穿透防御（df 减半计算） | 15 | 1 | 合体丹残方商店兑换 |
| 星渊破 | burst | 对带兽对手额外 +15% 伤害 | 60 | 4 | 征途天外层精英门 |

### 9.6 场景分化（举例）

| 场景 | 推荐逻辑（玩家自己发现）|
|---|---|
| 征途精英门（多连战）| 噬魂火（持续 dot 补刀）+ 净化心光（heal+条件触发）效率高 |
| 镇妖塔（单怪高 HP）| 混沌斩 + 天命一击（爆发集中）|
| PvP（对手带兽）| 星渊破对带兽对手优势明显 |
| 宗门战（高 df 守卫）| 太虚剑意（穿透防御）|

---

## 10. 存量内容接入与重调

### 10.1 PvE 门槛重调（M2，元婴及以上）

灵兽上线后存量门槛按有兽档复检：

- balance_sim 各境界 GEARED 档新增**有兽档**（同档常见兽、等级=上限 70%、0 星、资质 1.0）。
- 复检范围：元婴/化神/炼虚全部地图连战、秘境通关层、Boss 门槛、世界 Boss 击杀次数（20~80）。
- **"刚解锁可刷"判定不得因带兽而提前一个小阶以上**；越线的门槛上调怪物数值，幅度以"无兽满 buff 档仍维持原验收结论"为上界。
- 金丹及以下（无兽区间）零改动。

### 10.2 镇妖塔门槛（M4，上线时全档校准）

- 每层以"目标档位满 buff 有兽档刚好可刷"为口径，并入 balance_sim 有兽档矩阵。
- 无兽档"可努力打过"（防无兽玩家被塔拒于门外）。

### 10.3 PvP 天梯（M6）

- 决斗双方各自带出战兽（有就带，无则空）。
- 天梯 rating 不重置；兽是新的强度变量，赛季结算自然消化。
- 斗法台禁制对支援单位同等生效；控制预算断言（§4.6）在 PvP 固定 seed 下回归。
- 战报文案显示双方灵兽名及神通槽位摘要（增加阅读价值）。

### 10.4 宗门战（M6）

- 进攻据点战斗带出战兽；守卫 NPC 不带兽（守卫数值一次性上调补偿 +10%，可调）。
- 验收锚点：化神圆满有兽成员可稳胜守卫，金丹成员落败。

### 10.5 世界 Boss（M2 起）

- 出手即带兽，兽伤害计入个人贡献。
- 击杀次数跌出 20~80 区间时按档位上调 Boss HP。

---

## 11. 新角色与社交系统衔接

- **灵兽**：出师奖励追加 1 根绑定缚灵索 + 引导文案（出师即闻兽）。
- **奇遇**：链式奇遇终端可解锁稀有种子/神通残页线索；普通奇遇对道侣/师徒共同历练不加特殊 buff。
- **灵田**：道侣互赠白名单**不加入**种子和兽粮（白名单维持纯绑定小额丹药，防套现通道）。
- **探秘/镇妖塔**：个人行为；排行榜自然形成群内攀比，无需社交系统直接耦合。
- **七日引导**：征途（金丹解锁）和镇妖塔（炼气即可）各加入 1 条引导任务，完成得小奖。
- 共修/传功/双修与灵兽/奇遇/征途零耦合。

---

## 12. 数据模型新增 / 迁移

### 12.1 配置层

```
config.realms:     REALM_NAMES += ["合体期"];REALM_STAGES[6];STAMINA_CAP[6]=320;
                   STAMINA_REGEN_SECONDS[6]=121;SECLUSION_STAGE_HOURS[6]=960;
                   BIG_BREAKTHROUGH[6];_ANCHORS[6];_REALM_BASE_COST[6]=35_000_000

config.maps:       周天星海/阴阳鸿沟/寂灭深渊;元婴+地图追加灵兽出没+奇遇触发配置
config.dungeons:   jiutian=九天玄宫;expedition_tiers（征途档位配置:门权重/怪物池/奖励表）
config.bosses:     heti=噬星古鲲
config.shop:       STAMINA_BUY_BASE[6]=8000
config.recipes:    合体丹残方×4;缚灵索;兽粮;兽粮草→兽粮（炼器）;灵液（炼器）;合体装备(M1)
config.items:      合体丹入禁售名单;新材料(星海尘晶/阴阳玄玉/寂灭道痕/兽粮/兽魂×4档/
                   缚灵索/征途令/种子×6/灵液/神通残页×8/精品材料独立key×4)
config.beasts:     种族表;捕捉率;资质roll区间;等级上限;喂养曲线;星级消耗;守猎表;贡献预算常量
config.events:     **迁移扩展**现有 ENCOUNTERS；追加奇遇事件包(≥49条初版)；链式奇遇配置
config.skills:     新增8个合体期神通
config.estate:     洞府等级表(升级条件/格数/功能);种子配置表(含精品item_key)
config.tower:      镇妖塔层配置(怪物/首通奖励);无 game_flags 存储
config.auction:    kind 'beast' 底价表;稀有兽/天价播报阈值;精品灵田产出白名单
game_flags:        overflow_demote_grace_until 重写=v4 M0部署时刻+28天
APScheduler jobs:  新增周一0:00 job，重置全群 characters.tower_week_progress=0
```

### 12.2 表结构

```sql
-- ① 灵兽 --
spirit_beasts(id, owner_id, species_key, nickname, level, star, aptitude,
  status, hunt_end_at, hunt_hours, captured_at);
CREATE INDEX idx_beasts_owner ON spirit_beasts(owner_id, status);
CREATE INDEX idx_beasts_hunt  ON spirit_beasts(status, hunt_end_at);

hunt_daily(beast_id INTEGER, day TEXT, count INTEGER DEFAULT 0, PRIMARY KEY(beast_id,day));

-- ② 奇遇 --
adventure_sessions(id, user_id, event_key, step, ready_at, expire_at,
  status DEFAULT 'pending', notified_at, state);
CREATE UNIQUE INDEX idx_adv_active ON adventure_sessions(user_id) WHERE status='pending';

-- ③ 征途（命名与历练完全分开）--
expedition_runs(id, user_id, tier, floor, hp_snapshot, mp_snapshot,
  current_hp, current_mp, run_seed, current_doors, floor_settled,
  buffs, consumed_lifesaver, fail_count, rewards, started_at, status DEFAULT 'active');
CREATE UNIQUE INDEX idx_exp_active ON expedition_runs(user_id) WHERE status='active';

-- ④ 镇妖塔 --
tower_daily(user_id INTEGER, day TEXT, fail_count INTEGER DEFAULT 0, PRIMARY KEY(user_id,day));

-- ⑤ 洞府/灵田 --
estate_plots(id, user_id, slot, seed_key, planted_at, mature_at, is_mature, ling_used,
  UNIQUE(user_id, slot));

-- ⑥ 神通（M6 迁移）--
character_learned_skills(user_id INTEGER, skill_key TEXT, learned_at INTEGER,
  PRIMARY KEY(user_id, skill_key));
character_equipped_skills(user_id INTEGER, slot INTEGER, skill_key TEXT,
  PRIMARY KEY(user_id, slot));
-- M6 上线后废弃 character_skills（先并行运行一个版本，确认迁移正确后再删）

-- characters 迁移 (_ensure_column，幂等) --
-- active_beast_id INTEGER
-- beast_capture_streak INTEGER NOT NULL DEFAULT 0
-- debuff_until INTEGER
-- last_event_at INTEGER
-- tower_floor INTEGER NOT NULL DEFAULT 0
-- tower_week_progress INTEGER NOT NULL DEFAULT 0
-- estate_level INTEGER NOT NULL DEFAULT 0
-- extra_skill_slots INTEGER NOT NULL DEFAULT 0

-- big_fail_streak 无 schema 变更；读取范围扩展至 realm 5→6，更新注释与单测 --
-- auctions 无 schema 变更；kind 新增 'beast'，instance_id 指向 spirit_beasts.id --
```

### 12.3 模块落点

```
config/beasts.py       # 种族/捕捉/养成/守猎/预算常量
config/events.py       # 迁移扩展 ENCOUNTERS；追加奇遇事件包
config/estate.py       # 洞府等级表；种子配置表
config/tower.py        # 镇妖塔层配置

services/beast.py      # 捕捉/兽栏/喂养/进阶/放生/守猎/出战切换
services/adventure.py  # 奇遇触发/事件选项结算/链式进度
services/expedition.py # 征途（入口/选门/run结算/见好就收）；复用奇遇事件表
services/tower.py      # 镇妖塔挑战/进度/首通奖励（事务内比较tower_floor）/周排行
services/estate.py     # 洞府升级/灵田种植/惰性收获/施灵

services/combat.py     # simulate 支援单位扩展(a_support/d_support,默认None)
                       # _choose_skill 追加条件(heal<70%/stun-skip/shield-dedup)
                       # combat_slots_for(character) 动态函数（M6）

handlers/beast.py      # callback 前缀 beast:
handlers/adventure.py  # callback 前缀 adv:
handlers/expedition.py # callback 前缀 exp:  ← 不与现有 ex: 冲突
handlers/tower.py      # callback 前缀 tower:
handlers/estate.py     # callback 前缀 estate:

bot/app.py             # 注册 /beast /venture /tower /estate；
                       # 守猎到期/灵田成熟/链式奇遇到期并入现有1分钟通知任务；
                       # APScheduler 新增周一0:00 job（tower_week_progress 重置）
```

---

## 13. 平衡与验收

**验收档定义**（balance_sim profile，全部含满淬炼）：

| 档 | 内容 |
|---|---|
| 炼虚满 buff 档 | 炼虚圆满+满配+道途拉满+飞升被动满+本命满级，攻/存两极端 |
| 合体过渡档 | 合体初期锚点+现役炼虚装备/功法（M0 定参档）|
| 合体 GEARED 档 | 合体装备/功法（M1 起）|
| 有兽档矩阵 | 各境界 GEARED+同档常见兽（等级=上限 70%、0 星、资质 1.0）（M2 起）|
| 镇妖塔全层档 | 对应各层目标档位满 buff 有兽档（M4 上线时全档校准）|

**M0（合体开放）**

- 合体配置完整：6 个 realm 相关 config 项全含 realm 6。
- 炼虚圆满可尝试合体突破；缺丹返回 `need_pill`；失败轻惩罚不跌境。
- `big_fail_streak` 对炼虚→合体档生效（+10%/次，成功清零，≤95%），化神→炼虚档行为不回归破坏。
- 残方 ×4 合成合体丹（绑定）；签到保底发放路径。
- 合体门槛离散验收：炼虚满 buff 档可磨易图小怪、低效磨中图小怪；中图 Boss/难图 < 5%。
- 合体推进时长：普通 5~6 周、高活跃 3~4 周。
- 合体经济：灵石/精力产出 < 首买成本 25%；溢出分流 realm 6 档次切换；宽限期两侧时间点。
- 天人劫三段状态机；直面心魔选项覆盖天人劫。
- 降档宽限重写幂等；公告文案三处完整。

**M1（合体完整链路）**

- 九天玄宫：合体初期 40%~70%，合体圆满稳定通关。
- 合体 GEARED 档回归全部合体 PvE 门槛。
- 噬星古鲲：化神参与 + 合体参与两档验收；击杀次数 20~80。

**M2（灵兽核心）**

- 捕捉：胜利触发；超时遁走不计保底；streak 递增/清零/≤90%；兽栏满不触发；缺灵索拒绝。
- 兽栏与状态机：`active_beast_id` 与 status 同事务一致；兽栏 ≤3 并发插入事务兜底；hunt/auction 态全路径拒绝。
- 养成：等级上限随主人境界；星级消耗同档兽魂；放生返还兽魂绑定；交易后等级/星级/资质随实例保留。
- 守猎：4/8/12h 产出次线性；每兽每日 ≤2 次；产出全绑定无灵石；惰性结算幂等；守猎中不可出战。
- 支援单位引擎：`a_support/d_support=None` 时输出逐字节不变；兽在主人行动后出手；主人定身时兽蛰伏；`HARD_ROUND_CAP` 兜底不变。
- 贡献占比：满养兽 DPS ≤15%；兽疗每回合 ≤3% max_hp；定身无锁死（固定 seed 穷举）。
- 存量门槛复检："刚解锁可刷"不因带兽提前一小阶以上；无兽满 buff 档维持原结论。

**M3（奇遇系统）**

- 触发率与冷却：历练 8%/秘境 5%/签到 2%；**触发即计 4h 冷却**，不论玩家是否响应。
- 超时：15 分钟 token 过期后自动按保守选项结算；保守选项不扣资源不给奖励；链式超时按"缘分已散"结算（清除进度，无奖励）。
- 事件结算：success/failure_effects 分支正确；代价消耗原子性；受伤不致死；灵石损失不超档。
- chain_next 在选项级生效：不同选项走不同链式后续。
- 链式进度：unique index 保证同时只有 1 条；ready_at / expire_at / notified_at 三项正确持久化；通知任务在 ready_at 后推送 notified_at 写入。
- 大机缘概率 ≤1%；稀有灵兽出没走正常捕捉流程（不绕过 streak）。

**M4（征途模式 + 镇妖塔）**

- 征途：无每日次数上限，仅靠征途令限频；expedition_runs unique index 防重入。
- run 内 buffs 只活在 run 里（退出后清空）；战败 HP 恢复至 30% 后继续；护法符消耗标记幂等；强制退出奖励 ×70%；见好就收 ×100%；失败 3 次强制退出。
- current_doors 在层内稳定（重新打开面板不重新 roll 门）；floor_settled 防重复结算。
- 征途 + 灵兽：兽参与每层战斗；定身兽与精英门交互无控制链叠加。
- 镇妖塔：`tower_floor` 只进不退；首通奖励在事务内比较旧值发放（非 game_flags）；连续失败 3 次当日该层锁定；周一重置 `tower_week_progress=0` 正确执行。
- 镇妖塔全层档 balance_sim：目标档位满 buff 有兽档刚好可刷；无兽档"可努力打过"。
- **周事件总量审计（M4 上线前执行）**：新增每日主动操作目标 ≤5 分钟。

**M5（洞府/灵田）**

- 洞府升级：消耗原子性；等级上限 5；格数随等级解锁；重复升级请求幂等。
- 灵田：planted_at/mature_at 正确记录；惰性成熟（重复查看不重复发）；施灵精品用独立 item_key 且不绑定；一键收获/一键补种正确执行；成熟通知在 ready_at 推送一次（notified_at 防重发）。
- 精品产出价格纳入每周价格巡检；单格最大产出值 ≤ 当档历练 1 天收益 30%。

**M6（神通对决层 + 灵兽全面参战 + 收尾）**

- character_skills 迁移：数据完整；character_learned_skills + character_equipped_skills 均正确填充；旧表并行运行一版再删。
- `combat_slots_for(character)` 返回正确槽数；炼虚及以下仍 3 槽；simulate 确定性不变（固定 seed 回归）。
- heal/shield/stun/heal+ 技能条件：heal<70%才释放（满血跳过）；stun跳过已定身目标；战报展示跳过原因。
- 8 个新神通各有效果单测。
- PvP/宗门战带兽：控制预算 PvP 固定 seed 回归；宗门战守卫补偿 +10% 后验收结论不变。
- **周事件总量收尾审计**：确认新增每日主动操作目标不超过 5 分钟。

---

## 14. 四期里程碑

| 里程碑 | 范围 | 目标 |
|---|---|---|
| **V4-M0 合体开放** | v3 收口（§1.1）+ 合体配置/合体丹（残方+签到保底+失败累计保底）/三张历练图/合体突破（天人劫+直面心魔）+ 降档宽限重写与公告（上线前置验收）+ M1 线索预埋 | 炼虚圆满玩家获得合体突破与新地图成长目标 |
| **V4-M1 合体完整链路** | 九天玄宫/合体 Boss/合体装备材料炼制链/合体 GEARED 档回归 | 合体期 PvE、群 Boss 与装备链完整 |
| **V4-M2 灵兽核心** | 捕捉（缚灵索+递增保底）/兽栏/出战/喂养/支援单位引擎/PvE 参战（历练/秘境/世界 Boss）/有兽档矩阵+存量门槛复检 | 元婴+玩家获得"捕捉→养成→参战"完整闭环 |
| **V4-M3 奇遇系统** | 迁移扩展现有 ENCOUNTERS + 奇遇引擎（`services/adventure.py`）+ 初版事件包（≥49 事件）+ 链式奇遇 + 大机缘 + 负面结果全类型 | 历练/秘境有随机事件，每次行动带期待感；可玩性骨架建立 |
| **V4-M4 征途模式 + 镇妖塔** | 征途（四档/门系统/run 内增益/贪深惩罚/令牌制）+ 奇遇事件表复用（遇门）+ 镇妖塔（全层配置/首通奖励/周新推进榜）+ 灵兽参战 + 周事件总量审计 | 玩家有 Roguelike 决策体验；有永久攀塔进度可追求 |
| **V4-M5 洞府与灵田** | 洞府（5 级升级链/材料 sink）+ 灵田（6 种种子/惰性结算/施灵精品独立 item_key/一键收获补种）+ 通知任务接入 | 管理循环补完；合体材料有新长期 sink |
| **V4-M6 神通对决层 + 全面参战 + 收尾** | character_skills 迁移两张表 + `combat_slots_for` 函数 + 第 4 上阵槽（合体初期解锁）+ 8 个合体神通 + `_choose_skill` 基础条件 + PvP 天梯带兽 + 宗门战带兽（守卫补偿）+ 控制预算 PvP 回归 + 周事件总量收尾审计 + 文档/引导收尾 | 全部战斗场景带兽；技能 build 深度完整；系统融合收尾 |

**里程碑依赖顺序**：境界链先行（M0→M1）；灵兽（M2）在合体 M1 定参之后；奇遇底座（M3）必须在征途（M4）之前；神通两张表迁移（M6）在所有神通残页掉落机制（M4/M5）稳定后执行。

---

## 15. 暂不进入 v4.0 的内容

- 大乘、渡劫等更高境界（五期）。
- 跨群玩法全线：全服榜单/称号、跨群拍卖橱窗、跨群世界 Boss。
- 玩家间资源掠夺型生死斗（若未来做：双向确认+限额+冷却+不可夺绑定物+日志可查）。
- 灵兽繁殖/合成、灵兽装备、多兽同时出战、兽栏扩容（先观察单兽+3 栏运行数据）。
- 宗门远征（宗门共同 PvE 进度条）——留五期。
- 傀儡等其他横向养成轴。
- 本命法宝等级上限提升（维持 10 级）。
- 征途档位扩展（101 层+镇妖塔、新征途档位）——留五期按运营数据决定。
- 任何付费加速、付费突破、付费外观。

---
