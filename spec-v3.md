# 《问道》三期设计规格 (spec-v3)

> 在当前 main / v0.2 实现态（化神圆满 + 道途/转修 + 飞升点 + 周活动 + 宗门战 + 一口价坊市）基础上的三期设计。
> v3 规格范围：境界上限提升至炼虚圆满；补齐炼虚 PvE 与突破链；新增师徒、道侣、拍卖行、
> 心魔劫选项与本命法宝雏形。本文件只记录需要实现、迁移、验收或明确排除的技术规格。

文档状态：**设计草案，待评审后实施**。
数值标注"初版可调"者均需配合 `tools/balance_sim.py` 与上线数据继续调参。

---

## 0. v3 范围与边界

| # | 决定 | 取值 |
|---|---|---|
| 1 | 版本目标 | **化神圆满玩家获得炼虚阶段成长目标** |
| 2 | 境界上限 | v3 从化神圆满提升到 **炼虚圆满**；合体及以上留四期 |
| 3 | 社交系统 | 新增 **道侣 + 师徒**；师徒主目标为新角色承接 |
| 4 | 社交数值口径 | 轻数值（1%~5% 量级），**全部进现有 buff 合算 clamp**；不给战斗面攻防直接加成 |
| 5 | 玩家经济 | 坊市已稳定运行，v3 开 **拍卖行**：法宝实例 + 白名单高阶材料 |
| 6 | 变现 | 沿用 v2 决定：**不引入付费**，不在突破/转修/交易上做 pay-to-win |
| 7 | 跨群玩法 | 跨群 Boss / 全服战继续押后，本期不做 |
| 8 | 高风险 PvP | 生死斗继续押后，本期不做 |
| 9 | 横向养成 | **心魔劫选项（M1）+ 本命法宝雏形（M5）进 v3**；灵兽等重型新轴留四期 |
| 10 | 玩家存档 | 不重置、不回档；现有化神圆满玩家直接接入新内容 |

规格原则：

- **V3-M0**：先开放炼虚期、炼虚地图、炼虚突破材料来源，保证境界成长链可继续推进。
- **社交系统**：师徒提供新角色承接与传承奖励；道侣提供双向关系绑定、限周任务与共修机制。
- **玩家经济**：拍卖行承接坊市不收的带词条法宝实例，让高价值产出有价格发现机制。

---

## 1. 当前实现依赖

v2 落地的这些能力，三期必须复用而不是另起炉灶：

| 现有能力 | 三期复用方式 |
|---|---|
| `config.realms` 境界配置（含化神 realm 4） | 追加炼虚 realm 5：突破丹、阶段时长、属性锚点、成本 |
| 每境界三档历练图 + 低阶折叠 + 扫荡 | 给炼虚补 易/中/难 三张图，沿用难度、时长、遭遇数模型 |
| 前台活动窗口 `activity_windows` | 炼虚历练/秘境等前台动作照旧记录窗口，折算闭关收益（拍卖结算是后台任务，**不**记窗口） |
| 气血/法力出发快照结算 | 炼虚历练/秘境沿用，避免满血假象 |
| 交互式天劫 `tribulation_sessions` | 化神→炼虚改为"虚空劫"三段选择，复用状态机 |
| 世界 Boss 分档 + 按群内人数缩放 | 新增炼虚 Boss 档 `lianxu` |
| `callback_tokens` + `db.transaction()` | 结契/拜师/出师/拍卖出价与结算，全部走一次性 token + 原子事务 |
| `game_events` 领域事件 | 炼虚突破、出师、结契、天价成交进入播报/成就 |
| 绑定库存 `inventory(user_id, item_key, bound)` | 师徒/道侣/活动奖励默认 `bound=1`；绑定物不可上拍 |
| buff 合算 clamp（`config/buffs.py`：ATTACK/SURVIVAL 0.25、SECLUSION 0.60） | 双修、传承等新百分比加成一律接入 clamp，禁止旁路 |
| 溢出修为分流 `overflow_split`（版本顶点圆满：溢出 ×8% 道行；每累计 100000 溢出修为凝 1 飞升点，溢出来源周上限 14；次顶点圆满：×3% 道行、无飞升点） | 机制按 `len(REALM_NAMES)` 自动上移，追加 realm 5 即生效；道行周上限 600、飞升点凝点周上限 14 分别独立生效 |
| 坊市成交税 + 价格/频次审计 | 拍卖行复用同一套审计口径与脚本 |
| `tools/balance_sim.py` 多档 profile | 新增化神满 buff 档与炼虚 GEARED 档，门槛按满 buff 档调参 |

当前实现约束：

- 战斗引擎近二值：胜率通常 0% 或 100%，所有炼虚门槛用**可刷 / 门槛**口径描述与验收，不写字面胜率百分比。
- `reserve_stamina_for_action()` 拒绝闭关中行动；新前台动作若允许后台闭关并行，记录 `activity_windows`，不要直接复用该函数。
- 高境界"裸装"（锚点强、装备占比低）是 v2 平衡大改的遗留课题；炼虚 M0 的验收 profile 必须是**炼虚初期锚点 + 现役化神装备/功法**，不能拿裸锚点调参。
- 经济反套利空间窄：任何新增可交易产出（尤其拍卖行白名单材料）都要进 balance_sim 反套利校验。

### 1.1 v2 收口项（M0 前置）

三期动工前先完成两个 v2 收口项：

1. **issue #45**：补齐元婴及以上的常规道行来源（历练/秘境/Boss/宗门任务的道行发放对齐 spec-v2 §4.3）。
2. **满淬炼档进 balance_sim**：道途淬炼满级的属性档纳入回归 profile，后续所有炼虚门槛都要在含满淬炼的满 buff 档下验收。

---

## 2. 三期核心循环

```
  化神圆满
      ↓
  [炼虚突破] 炼虚丹 + 虚空劫
      ↓
  [炼虚期] 新地图 / 新秘境 / 炼虚 Boss / 炼虚装备炼制
      ↓                       ↘
  [炼虚圆满后] 溢出修为分流上移    [师徒] 收徒传道 → 出师奖励
  （道行 / 飞升点沿用 v2 sink）    [道侣] 结契 → 双修 / 互赠 / 专属播报
      ↓
  [拍卖行] 高价值法宝实例与白名单材料的价格发现
      ↺
  继续挂机、历练、Boss、宗门战、周活动
```

目标：炼虚开放后，成长循环（境界→道途→飞升点）完整上移一层；社交系统提供关系绑定、
限周收益与可审计奖励；拍卖行提供高价值物品交易与资金 sink。

---

## 3. 境界上限扩展：炼虚期

### 3.1 新境界结构

| 大境界 | 小阶 | 主要解锁 |
|---|---|---|
| 化神期 | 初 / 中 / 后 / 圆满 | 当前 v2 上限 |
| **炼虚期** | 初 / 中 / 后 / 圆满 | 炼虚地图、虚空神殿秘境、炼虚 Boss、炼虚装备、溢出分流上移 |
| 合体期+ | 待定 | 四期 |

配置改动（均为初版可调）：

- `REALM_NAMES` 追加 `炼虚期`；`REALM_STAGES[5] = ["初期", "中期", "后期", "圆满"]`。
- `STAMINA_CAP[5] = 280`。
- `STAMINA_BUY_BASE[5] = 6000`，当日首买 20 精力约 300 灵石/精力。
- `SECLUSION_STAGE_HOURS[5] = 720`，T0.11 数值定稿覆盖初案 144，用于把炼虚推进压到 6~9 周。
- `_REALM_BASE_COST[5] = 4_000_000`，`_STAGE_MULT` 沿用 1.20。
- `BIG_BREAKTHROUGH[5] = {"pill": "炼虚丹", "base_rate": 0.45, "tribulation": True}`。

### 3.2 炼虚属性锚点

当前化神圆满基础锚点 `hp=52000 / mp=4200 / atk=3800 / df=2800 / spd=900 / crit=280`。
沿用现有跨境界倍率曲线（初阶≈上一境圆满×1.5，圆满≈本境初阶×2.2）：

| 境界 | 气血 | 法力 | 攻击 | 防御 | 身法 | 暴击 |
|---|---:|---:|---:|---:|---:|---:|
| 炼虚初期 | 78000 | 6300 | 5600 | 4100 | 1300 | 420 |
| 炼虚圆满 | 170000 | 13500 | 12000 | 8800 | 2300 | 700 |

平衡目标（全部按"可刷 / 门槛"口径）：

- 化神圆满含满配、满淬炼、单条道途拉满、飞升被动满级的**满 buff 档**，可刷炼虚易图普通小怪（允许"磨"）。
- **过渡档**：化神圆满满 buff 档还应能**低效磨炼虚中图普通小怪**（高血蓝消耗、
  单位精力收益明显差于易图，但不为 0），用于验证炼虚易图到中图之间存在可感知但低效的过渡带。
- 炼虚中/难图的 **Boss 与多遭遇连战**是离散门槛，必须压住化神圆满满 buff 档（挑战成功率 < 5%）。
- 炼虚初期刷易图稳定、中图小怪可刷；中图 Boss、难图分别作为炼虚中期、炼虚后期的成长门槛。
- 炼虚圆满打化神内容明显碾压，但化神内容收益不得成为最佳成长路线。

**炼虚期推进时长验收目标**：

- 普通活跃档（根骨 60、有效闭关 18h/日、精力大部分投炼虚图 + 每日虚空神殿）：
  炼虚初期 → 圆满 **6~9 周**；高活跃档下限 **≥ 4 周**。
- 加上首破期望 4~6 周（§3.4），化神圆满玩家从 M0 上线到炼虚圆满的整体版本周期约 **10~15 周**。
- 超出区间时的调参手段：`SECLUSION_STAGE_HOURS[5]` 与炼虚地图修为，二者联动重调。

装备验收口径：

- M0 没有炼虚装备，三张图的验收 profile 是**炼虚初期锚点 + 现役化神装备/功法**。
- M1 炼虚装备上线后，`tools.balance_sim` 新增炼虚 GEARED 档并回归重调全部炼虚门槛。
- 满 buff 档沿用 v2 方法论，至少拆攻击极端（剑修圆满）与生存极端（体修圆满）两档，且都含满淬炼。

### 3.3 化神→炼虚突破

| 目标境界 | 丹药 | 基础成功率 | 额外试炼 |
|---|---|---:|---|
| 炼虚期 | 炼虚丹 | 45% | 虚空劫 |

规则：

- 触发条件：化神圆满 + 修为达到突破成本 + 炼虚丹。
- 成功率沿用 `big_success_rate` 结构：基础率 + 根骨修正 + 法宝/道途/丹药加成，上限 95%。
- 失败轻惩罚：损失当前修为 30%，道基不稳，不跌境。
- **失败累计保底（新增）**：炼虚突破每失败一次，下次基础成功率 +10%（45% → 55% → 65% → …，
  总率仍受 95% 上限），突破成功后清零。数据落点：`characters` 增加 `big_fail_streak` 列
  （`_ensure_column`，通用于大突破，初版仅炼虚突破读取）。按此计算期望突破次数 ≈ 1.9 次，
  压掉 45% 单次率的随机长尾。
- 虚空劫复用 `tribulation_sessions` 三段状态机，文案换虚空/肉身崩解主题；选项初版沿用
  纯防御/回复三选（凝守本源 / 祭护体法宝 / 服大还丹）。"心魔"类奖励倍率选项作为 M1 横向内容落地，
  见 §7.1。

### 3.4 炼虚丹来源

炼虚丹必须与 M0 同步提供来源：

- 化神难图 `天外古墟` 极低概率掉落 `炼虚丹残方` 或 `炼虚丹`。
- 秘境 `太虚天门` 深层掉落完整丹方/材料；M1 后新秘境 `虚空神殿` 追加来源。
- 化神/炼虚世界 Boss 前列奖励掉材料，参与奖不直接掉完整丹。
- 炼虚易图低概率掉落炼虚丹材料（仅作补充，见下方权重原则）。
- NPC 商店不直售炼虚丹；**坊市/拍卖行均不可上架炼虚丹**（沿用化神丹的禁售名单）。

**首破来源权重原则**（防"先有鸡还是先有丹"死锁）：

- 第一枚炼虚丹的期望获取路径必须 **100% 落在化神可及内容**（化神难图、太虚天门、化神 Boss）；
  balance_sim 验收口径：化神圆满玩家按正常日程（每日太虚 2 次 + Boss + 难图历练）刷齐首枚
  炼虚丹的期望周期落在 **2~4 周**。
- 炼虚图掉落的丹材只用于加速**已突破玩家**炼后续丹（帮群友、二次突破失败补给），
  不得成为任何丹材的唯一来源。

**期望成功突破周期**（验收以此为准，不只看首枚丹）：

- 45% 基础率 + 失败累计保底（§3.3）下期望消耗 ≈ 1.9 枚炼虚丹；首枚 2~4 周，
  后续每枚因材料管线已建立按 1~2 周计——**化神圆满玩家的期望"进炼虚"周期为 4~6 周**，
  balance_sim 按该口径整体校验，超出即调掉率或保底参数。
- **残方合成保底**：`炼虚丹残方` 为化神难图/太虚天门的稳定小概率掉落，集齐 4 枚可直接
  合成 1 枚炼虚丹（绑定）。残方管线保证极端脸黑玩家也有确定性的丹药进度条。

**M0 预置 M1 数据**：M0 上线时同步在化神难图/太虚天门低概率掉落 `炼虚装备图纸残页`，
并在炼制界面展示"待解锁"的炼虚装备/丹方条目，作为 M1 炼虚炼制链的前置产出。

### 3.5 炼虚历练地图

| 地图 | 难度 | 精力 | 时长 | 定位 | 独占掉落 |
|---|---|---:|---|---|---|
| 太初雾泽 | 易 | 20 | 15~18 分钟 | 炼虚入门稳定刷 | 雾泽虚砂 |
| 虚空裂海 | 中 | 24 | 18~22 分钟 | 炼虚中期主刷 | 裂海空髓 |
| 混沌古狱 | 难 | 28 | 22~26 分钟 | 炼虚后期高风险 | 混沌残核 / 炼虚丹材料 |

掉落口径：

- 炼虚首买精力约 300 灵石/精力；炼虚地图灵石/精力产出比首买成本**至少低 25%**，延续反套利红线。
- 修为收益明显高于化神图，防止回头刷低阶成为最优。
- 独占材料用于炼虚丹、炼虚装备、道途宗师进度、飞升点材料。

### 3.6 炼虚秘境：虚空神殿

| 配置 | 初版取值 |
|---|---|
| 解锁 | 炼虚初期 |
| 层数 | 6 |
| 精力 | 70 |
| 入场费 | 4000 灵石 |
| 每日次数 | 2 |
| 主要产出 | 炼虚丹材料、炼虚法宝、道途/飞升材料、拍卖行白名单材料 |

- 炼虚初期推进 40%~70% 层数；炼虚圆满稳定通关。
- 太虚天门对炼虚玩家自然降级为次级日常，不做废弃。

### 3.7 炼虚世界 Boss 档

新增 Boss 档 `lianxu`：

| Boss | 面向境界 | 持续 | 精力 | 奖励定位 |
|---|---|---:|---:|---|
| 吞虚魔蟒 | 炼虚 | 2 小时 | 20 | 炼虚材料、道途材料、飞升点材料 |

沿用同群独立、按群内已知修仙者人数缩放、参与奖 + 贡献奖 + 前列奖励的现行模型。

### 3.8 长线系统上移

- **溢出分流**：机制随版本顶点自动上移——**炼虚圆满**：溢出 ×8% 转道行；溢出修为每累计
  100000 凝成 1 飞升点，不足部分跨结算保留，溢出来源每周最多凝成 14 点。**化神圆满未突破**：
  自动降为次顶点档（×3% 道行、无飞升点转化）。`OVERFLOW_DAOHANG_WEEKLY_CAP = 600`
  与飞升点周上限分别独立生效，跨宽限顶点档和当前顶点档共用各自额度。
- **降档宽限期**：M0 上线起 **4 周内**，化神圆满未突破玩家仍按顶点档
  （8% 道行 / 每十万溢出修为凝 1 飞升点）分流；宽限期满自动落入次顶点档（3% / 0）。宽限截止**不写死在代码**：
  M0 首次启动迁移时写入 DB kv（`game_flags` 表，key `overflow_demote_grace_until`）=
  **部署时刻 + 28 天**；读侧 `settle` 缓存该值。宽限期与首破期望周期
  （4~6 周）衔接：按期推进的玩家可在降档生效前后突破炼虚并恢复完整分流。
- **公告为 M0 上线前置验收项**：版本公告 + `/help` + 收功文案三处明示"宽限截止日期、
  降档后档位、突破炼虚可恢复"；面板/收功界面展示玩家当前所处分流档位。缺公告文案 M0 不得上线。
- **道途**：炼虚内容按 v2 口径给道行；宗师级进度以炼虚材料 + 飞升点推进。
- **飞升试炼**：解锁条件保持化神圆满不变；降档只影响溢出转化，不影响试炼与被动；
  炼虚玩家试炼收益略增但受周上限约束。
- **飞升被动**：硬上限维持 5 级不在 v3 提升，避免 clamp 空间被提前吃满。
- **叩问天门**：四项飞升被动全部圆满后开放。玩家可将飞升点部分或全部投入天门，下一重费用为
  `100 × 2^当前重数`；投入进度永久保留、不可撤回。天门不提供循环属性成长，3/6/9/12 重只发
  一次性绑定奖励并触发里程碑播报，12 重以后仅保留重数、称谓与榜单展示。

---

## 4. 师徒：新角色承接与传承链

### 4.1 定位

师徒系统用于承接新注册角色，并给高境界玩家提供非战斗数值的长期成就线（桃李称号）。

### 4.2 拜师规则

- 师父：元婴期及以上；徒弟：**筑基圆满及以下**（境界差硬门槛，防止既有高境界账号互拜刷奖励）。
- 一位师父同时最多 3 名徒弟；一名徒弟同一时间只有一位师父。
- 拜师流程：任意一方发起，**双向确认**（`callback_tokens` 一次性 token），写入即生效。
- 解除：任意一方可单方解除；解除后双方各有 7 天冷却才能建立新师徒关系；
  已发放的里程碑奖励不回收，但该徒弟对该师父的后续里程碑不再计数。
- **重拜代价必须公示**：解除后重拜新师父，出师所需的累计活跃天数**从 0 重计**（§4.3）。
  拜师与解除的确认界面文案都要明示这一条，让徒弟在知情下选择，不做隐藏规则。

### 4.3 传承收益

| 对象 | 收益 | 约束 |
|---|---|---|
| 徒弟 | 修炼效率 +5%（出师前有效） | 计入 SECLUSION clamp（0.60），不上调 clamp 总量 |
| 徒弟 | 师父每日可"传功"一次：徒弟获得小额修为 | 额度按徒弟境界定，师父无消耗；每对师徒每日一次；**徒弟当日有闭关/历练/秘境/任务任一行为才可领取**（防止仅签到触发收益） |
| 师父 | 徒弟突破大境界（金丹/元婴）时获得道行 + 播报 | 每名徒弟每个里程碑一次性 |
| 师父 | **周活跃回报**：每周结算按徒弟本周活跃天数发小额道行 | 每徒弟每周封顶（初版 5 道行/活跃日、上限 30/周/徒弟，可调）；计入道行周上限，不新增资源入口；**出师后停发**，后续收益需来自新的 active 徒弟 |
| 双方 | **出师**：徒弟达到元婴初期 **且拜师后累计活跃天数 ≥ 8**（有任一前台行为记 1 活跃天） | 双方一次性奖励：道行 + 绑定材料 + 称号进度 |

出师活跃天数门槛按当前成长模拟锚定（闭关 + 历练双通道到元婴初期：
最高活跃约 8 天、普通活跃约 11 天、低活跃约 18 天，故初版取 8）。原则：正常最快路径
不受门槛约束，只拦被资源灌喂或纯脚本速通的异常号。后续若调整成长曲线，此门槛随最高活跃档
到元婴初期的天数重算。

- 师父的桃李称号线：累计出师 1 / 3 / 5 名徒弟解锁递进称号，`game_events` 播报。
- 所有师徒奖励**默认绑定**（`bound=1`），不可交易、不可上拍。
- **clamp 截断透明化**：徒弟的 +5% 传承加成若因 SECLUSION clamp（0.60）已满而被截断，
  收功文案需显示"已达闭关增益上限"，不能让玩家以为加成静默失效是 bug。

### 4.4 新角色引导（onboarding，与师徒配套）

新角色引导与师徒系统配套，用于降低注册早期资源缺口：

- **七日引导任务链**：注册起 7 天，每天 1~2 个教学任务（首次历练/炼丹/上架坊市/加入宗门…），
  奖励绑定丹药、灵石、精力，覆盖前期最陡的资源缺口。
- **师徒联动**：徒弟完成引导任务时师徒双方各得小额奖励，建立教学行为与奖励的直接关联。
- 引导任务奖励全绑定，不进交易；只对新注册角色开放，既有角色不可补领。

### 4.5 反滥用

- 境界差硬门槛 + 出师需元婴 **加** 8 活跃天双条件 + 奖励一次性：异常账号必须达到元婴并持续活跃，
  时间成本即主要反滥用成本。
- 同一 `(师父, 徒弟)` 对的每个里程碑终身只发一次，解除重拜不重置。
- 传功需徒弟当日活跃才可领取，且修为额度远低于徒弟同时间正常闭关收益的 50%，
  只是"加速"不是"代练"。
- 周活跃回报与传功共用同一"当日活跃"判定：触发周回报必须每天产生前台行为记录；
  且回报计入道行周上限，3 名满勤徒弟的合计回报也只占周上限的一小部分，无套利空间。
- 出师奖励不含灵石与可交易物，杜绝灵石搬运。

---

## 5. 道侣：双向关系绑定

### 5.1 结契规则

- 双方均达到金丹期；一人同一时间只有一位道侣。
- 结契需消耗 1 枚 `同心结`（炼器可制 / 周活动商店兑换，**绑定**），流程双向确认（一次性 token）。
- 解除：任意一方可单方解除，消耗灵石（初版 50000，可调）；解除后双方 7 天冷却才能再结契。
- 结契 / 解除均入 `game_events`，群内播报（解除播报低调文案）。

### 5.2 道侣收益

| 收益 | 量级 | 约束 |
|---|---|---|
| 双修闭关：双方同时处于闭关状态的时段，闭关效率 +5% | 轻数值 | 计入 SECLUSION clamp（0.60），与宗门/道途/飞升/据点竞争同一上限 |
| 每日互赠：每日一次向道侣赠送 1 件小额物品（绑定丹药/材料白名单） | 极小额 | 白名单固定、绑定物、不可反向套现 |
| 专属称号 + 面板展示 + 合击 Boss 时的专属播报文案 | 无数值 | — |
| 周活动双人任务：**同一周内双方各自完成指定活动即算达成**（异步，不要求实时协作），得少量道行 | 限周 | 道行计入活动周上限，不新增上限额度 |

双修结算与感知要求：

- 双修加成必须在**两条收功路径统一生效**：主动出关收功，以及"返回面板即自动收功"的惰性结算。
  实现层面即在 `settle` 的闭关结算里按双方闭关时段的**重叠区间**折算，与入口无关。
- **重叠的判定只看闭关区间，不看在线状态**：离线自动闭关时段同样计入。算法：结算本方
  `[start_at, now]`（先按 `OFFLINE_CAP_HOURS` 截断）时，查道侣在该区间内的闭关区段——
  进行中的闭关取 `[partner_start_at, now]`，已结束的取其落库区间——求交集秒数，
  该秒数部分额外 +5%（先截断、后求交，双方各自独立结算，互不触发对方收功）。
- 道侣**进行中**闭关的开始时间戳已落库（`characters.seclusion_at`，0=未闭关），读侧用
  `db.fetchone` 即可，无新表；但已结束闭关的历史区间当前**未保留**，需在收功时把最近一次
  闭关区间写回（`last_seclusion_start/end` 两列，见 §8.2，只为对方结算窗口内的交集查询服务，
  不做全量历史）。
- 收功文案必须展示重叠收益，否则玩家无感知：例如
  `与道侣同参 137 分钟，双修额外修为 +xxx`。无重叠时段则不展示该行。
- **clamp 截断透明化**：双修 +5% 被 SECLUSION clamp 截断时，收功文案显示"已达闭关增益上限"
  （与 §4.3 传承同一处理）。

### 5.3 同步共修仪式（主动协作玩点）

在被动的"重叠折算"之外，新增一个要求双方确认的限周机制。

| 项 | 初版取值 |
|---|---|
| 参与关系 | 道侣或师徒（`social_bonds` 两种 kind 通用） |
| 频次 | 每人每周 1 次（跨关系不叠加：本周与道侣共修过就不能再与徒弟共修） |
| 流程 | 一方发起邀请（一次性 token），另一方 **10 分钟内确认**即同步开始，共修 30 分钟 |
| 前置 | 双方均不在重伤/其他前台活动中；共修计前台活动窗口（`activity_windows`） |
| 收益 | 结束后各得一次性修为包，量级 ≈ 各自境界 2~3 小时闭关收益；师徒场合徒弟得修为、师父得少量道行 |
| 口径 | **独立限周奖励，不进 SECLUSION clamp**；不与被动 buff 竞争额度 |

该机制作为限周主动协作收益，错过不补，不作为每日必做收益来源。

**不做范围**：任何战斗面攻防加成、同图历练加成、PvP 加成。战斗引擎近二值，
道侣不能成为翻越门槛的隐形乘区。

### 5.4 防滥用

- 结契/解除冷却 + 解除费用，抑制频繁解除/重结契刷双人任务。
- 互赠白名单只含绑定小额物品，赠送后仍为绑定，堵灵石/材料搬运通道。
- 双修加成只在双方**同时闭关**时生效，按重叠时段折算，不可单方触发。

### 5.5 社交链数据

师徒与道侣共用一张关系表（见 §8.2 `social_bonds`），kind 区分，状态机统一：

```
pending ─┬─ 确认 → active ─ 解除 → dissolved   （触发双方 7 天冷却）
         ├─ 拒绝 → declined                    （不触发冷却，双方可立即发起新关系）
         └─ 48h 超时 → expired                 （同上，不触发冷却）
```

冷却只挂在 `dissolved` 上：pending 被拒或超时**不消耗任何冷却**，这是防止"拒绝别人
反而锁死自己"的明确语义，服务层不得各自解释。唯一索引只覆盖 `pending/active` 两态。

---

## 6. 拍卖行：法宝实例与高阶材料的价格发现

### 6.1 定位与范围

一口价坊市已稳定运行，但**带词条的法宝实例**无法定价（词条差异大），一直不收。
拍卖行专门承接。拍卖行里程碑为 **V3-M2**，紧跟炼虚完整链路，独立开发独立验收
（并发/托管/审计不与其他系统混做）；M2 上线前，白名单高阶材料沿用现有一口价坊市交易；
M2 后双渠道并存。拍卖行承接范围：

- **可拍**：法宝实例（带词条、含强化等级）；白名单高阶材料（天外残玉、混沌残核等，名单配置化）。
- **不可拍**：绑定物（`bound=1`）；炼虚丹/化神丹/转修令/保命符等坊市禁售名单同样适用；普通材料与丹药继续走坊市。

### 6.2 拍卖机制

| 项 | 初版取值 |
|---|---|
| 模式 | 英式拍卖，可选一口价（buyout） |
| 拍期 | 24 小时 |
| 起拍价 | 卖家自定，≥ 系统底价（按品阶配置） |
| 加价幅度 | ≥ 当前价 5% |
| 防狙击 | 结拍前 5 分钟内出价，结拍时间顺延 5 分钟（最多顺延 6 次） |
| 挂拍费 | 起拍价 1%，不退（防低质量挂拍） |
| 成交税 | 10%（与坊市税率独立配置，初版可调） |
| 同时挂拍上限 | 每人 3 件 |
| 撤拍 | 无人出价时可撤，挂拍费不退 |

**一口价（buyout）细则**：

- 卖家可选设置，须满足 `buyout ≥ 起拍价 × 1.2`（防止用一口价名义绕开竞价定锚）。
- 出价满 buyout 即**立即成交**：同一事务内实扣买家灵石、退还当前最高价者 escrow、
  法宝过户、卖家收款（扣税，税率与普通成交一致 10%）、拍卖置 `sold` 结束——
  **不进入防狙击顺延**，顺延只服务于竞价结拍。
- 英式加价的出价额**不得达到或超过 buyout**：出价 ≥ buyout 时按 buyout 一口价路径成交，
  多余部分不收。
- buyout 成交同样落 `auction_bids` 与事件日志，天价播报阈值照常适用。

### 6.3 托管与实例锁定

法宝实例上拍必须**实际托管**，不能只做标记：

- `item_instances` 增加 `status` 列（`_ensure_column`，默认 `'normal'`）；上拍时校验未装备
  （`equipped_slot IS NULL`）并置为 `'auction'`。
- `status='auction'` 的实例在**装备、强化、重铸、分解、赠送、再次上拍**全部路径中排除
  （服务层统一在实例读取入口过滤，不允许各调用点自行判断）。
- 材料类沿用坊市模型：上架即扣库存，流拍/撤拍退回。
- 成交过户 / 流拍退回时恢复 `status='normal'`，与灵石结算同一事务。

### 6.4 资金与结算

- 出价即**实扣**：在同一 `db.transaction()` 内从 `characters.spirit_stone` 扣款写入
  `auction_escrow`，不是只记"冻结额"——余额永远反映真实可用灵石。被超价时原子退回。
- 结拍由 APScheduler 定时任务驱动，**结算幂等**：`auctions.status` 状态机单向
  （`active → sold/passed/cancelled`），重复触发结算必须是 no-op。
- 成交：法宝过户 + escrow 转卖家（扣税）+ 事件播报；流拍：物品退回 + escrow 全额退款。
- 全流程 `db.transaction()` 原子化 + `callback_tokens` 一次性确认；并发出价串行化，
  不允许超卖/双花；出价、顺延、结算全量落 `auction_bids` / 拍卖事件日志。

### 6.5 审计与反套利

- 复用坊市审计口径：异常价（明显高/低于历史区间）、高频对倒（同两账号反复互拍）出审计日志。
- 拍卖行只转移灵石不产生灵石；挂拍费 + 成交税是净 sink。
- 白名单材料进 balance_sim 反套利校验：材料可拍不得打穿"买精力→刷钱"红线。
- 天价成交（阈值配置化）进 `game_events` 群播报，作为群内可见的交易审计信号。

**低流动性群处理**：

- 多人 bot 的小群里，拍卖可能沦为"挂满拍期无人出价"。初版先落两个低成本曝光手段：
  新拍上架与**结拍前 1 小时**各推一次群播报提醒；玩家可"关注"某场拍卖，被超价/临近结拍
  收到私聊通知（复用现有通知任务）。
- 跨群展示/全服拍卖橱窗涉及跨群数据聚合，与 §0 第 7 条一致**留四期评估**，本期只留接口余地
  （拍卖数据不做群内隔离假设）。

---

## 7. 横向养成：心魔劫与本命法宝

v3 放入两个轻量横向内容：M1 的心魔劫选项与 M5 的本命法宝雏形。灵兽等重型新轴留四期。

### 7.1 心魔劫选项（M1）

金丹起的所有交互式天劫（天劫 / 神魂劫 / 虚空劫），第三段新增可选项：

| 选项 | 效果 |
|---|---|
| **直面心魔** | 本段**不获得任何减伤**（高风险）；渡劫成功则突破后获得 `道心通明`：24 小时闭关效率 +10%（限时 buff，计入 SECLUSION clamp）+ 少量道行；渡劫失败在常规惩罚外**额外损失 5% 当前修为** |

- 实现：`tribulation_sessions` 增加奖励状态字段（`reward_flag`，`_ensure_column`），
  结算时读取——这正是 v2 当时评估"超出纯防御/回复状态机"而延后的扩展，本期落地。
- 定位：风险自选的一次性突破选项，不建立长期养成轴；实现范围为一个选项、一列状态与对应文案，
  渡心魔成功/失败均播报。

### 7.2 本命法宝雏形（M5）

本命法宝为与境界正交的长期成长维度，要求跨版本保值，并与器修道途协同。

骨架规则（M5 启动前补 detail 文件）：

- **认主**：元婴期起，可将一件金丹品阶及以上、未上拍的法宝实例"滴血认主"为本命法宝；
  仪式消耗材料 + 灵石。认主后该实例**永久绑定**：不可交易、不可上拍、不可分解。
- **解缚**：可花高额灵石"斩缚"解除，但本命等级清零；解除成本应高于常规装备更换成本。
- **成长**：喂养材料/器魂提升本命等级（初版上限 10 级），每级小幅提升该法宝主属性；
  加成计入 ATTACK/SURVIVAL clamp（0.25），**不旁路**全局属性计算。
- **协同**：器修道途提升喂养效率/降低成本，作为器修专属长期材料 sink。
- **数据骨架**：`characters += natal_instance_id`、`item_instances += natal_level`
  （均 `_ensure_column`）；认主实例天然排除在拍卖托管之外（绑定语义优先）。

验收红线：本命加成进 clamp 回归；认主实例不可进任何交易路径；喂养材料消耗进
balance_sim（它是新的材料 sink，反过来支撑炼虚图/秘境的长期刷图价值）。

---

## 8. 数据模型新增 / 迁移

### 8.1 配置层

```
config.realms:
  REALM_NAMES += ["炼虚期"]
  REALM_STAGES[5]；STAMINA_CAP[5] = 280
  SECLUSION_STAGE_HOURS[5] = 720
  BIG_BREAKTHROUGH[5] = {"pill": "炼虚丹", "base_rate": 0.45, "tribulation": True}
  _ANCHORS[5]；_REALM_BASE_COST[5] = 4_000_000

config.maps:      太初雾泽 / 虚空裂海 / 混沌古狱
config.dungeons:  xukong = 虚空神殿
config.bosses:    lianxu = 吞虚魔蟒
config.shop:      STAMINA_BUY_BASE[5] = 6000
config.auction:   白名单材料、底价、税率、拍期、防狙击参数、buyout 最低倍率(1.2)
config.bonds:     师徒里程碑奖励表、传功额度、出师活跃天数门槛、周活跃回报额度/封顶、
                  道侣互赠白名单、双修加成、共修仪式参数（时长/收益/限周）、新角色引导任务链
                  （模块名取 bonds：config/social.py 已被 v2 群播报/DM 通知占用）
config.crafting:  炼虚丹残方 ×4 合成配方（产物绑定）
config.natal:     本命法宝认主消耗、喂养成本曲线、等级上限(10)、斩缚费用（M5 前占位）
config.events:    心魔劫选项参数（道心通明时长/幅度、失败额外损失 5%）——落现有天劫选项
                  （TRIBULATION_ACTIONS）所在模块，不新建 config/tribulation

game_flags(kv):   overflow_demote_grace_until = M0 首启部署时刻 + 28 天（§3.8，不写死代码）

tools.balance_sim:
  CONTENT_REALM 增加炼虚内容
  新增 化神圆满满 buff 档（满淬炼 + 单条道途拉满 + 飞升被动满，收口 §1.1）
  新增 炼虚初期 + 现役化神装备档（M0 验收）
  M1 后新增 炼虚 GEARED 档
```

### 8.2 表结构

```
social_bonds(
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  kind TEXT NOT NULL,              -- 'mentor' | 'partner'
  a_id INTEGER NOT NULL,           -- mentor: 师父；partner: 本方（镜像双行，见下 ②）
  b_id INTEGER NOT NULL,           -- mentor: 徒弟；partner: 对方
  status TEXT NOT NULL,            -- 'pending' | 'active' | 'dissolved' | 'declined' | 'expired'
                                   --   冷却只挂 dissolved；declined/expired 不触发冷却（§5.5）
  created_at INTEGER NOT NULL,
  expires_at INTEGER,              -- pending 48h 过期时间戳，结算任务置 expired
  activated_at INTEGER,
  dissolved_at INTEGER,            -- 冷却查询依据：user 最近一次 dissolved_at + 7d
  active_days INTEGER NOT NULL DEFAULT 0,  -- mentor 行：徒弟拜师后累计活跃天数（出师门槛数据落点）
  last_active_day TEXT             -- mentor 行：最近记入的活跃日 'YYYY-MM-DD'（Asia/Shanghai），当日去重
)
-- active_days 累计规则：徒弟当日首个前台行为（闭关收功/历练/秘境/任务）时，
-- 若 last_active_day != 今日则 active_days += 1 并更新 last_active_day；
-- 与"传功当日活跃校验"（§4.3）共用同一判定，写入走同一事务。解除后计数冻结，重拜新师父从 0 起。

-- 规则必须由数据库兜底，不能只靠服务层：
-- ① 徒弟同一时间只有一个师父（pending 也占位，防并发双拜）：
CREATE UNIQUE INDEX idx_bonds_mentor_b ON social_bonds(b_id)
  WHERE kind='mentor' AND status IN ('pending','active');
-- ② 道侣一人一侣：partner 关系写成**镜像双行**（a→b 与 b→a 同事务成对写入/变更），
--    这样"每人至多一位道侣"可用单列部分唯一索引精确表达
--    （规范化单行 + a/b 两列索引兜不住同一人一次当 a、一次当 b 的交叉场景）：
CREATE UNIQUE INDEX idx_bonds_partner_a ON social_bonds(a_id)
  WHERE kind='partner' AND status IN ('pending','active');
-- ③ 师父 active 徒弟 ≤ 3：唯一索引无法表达，在 db.transaction() 内 count 后插入
--    （BEGIN IMMEDIATE 写锁串行化即为兜底）。
-- 常规查询索引：
CREATE INDEX idx_bonds_a ON social_bonds(kind, a_id, status);
CREATE INDEX idx_bonds_b ON social_bonds(kind, b_id, status);

bond_milestones(                   -- 师徒里程碑一次性发放记录（终身去重）
  bond_kind TEXT NOT NULL,
  a_id INTEGER NOT NULL,
  b_id INTEGER NOT NULL,
  milestone TEXT NOT NULL,         -- 'jindan' | 'yuanying' | 'graduate' | ...
  granted_at INTEGER NOT NULL,
  PRIMARY KEY(bond_kind, a_id, b_id, milestone)
)

auctions(
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  seller_id INTEGER NOT NULL,
  kind TEXT NOT NULL,              -- 'equipment' | 'material'
  item_key TEXT, instance_id INTEGER, qty INTEGER,
  start_price INTEGER NOT NULL,
  buyout INTEGER,
  current_bid INTEGER, current_bidder INTEGER,
  end_at INTEGER NOT NULL,
  extend_count INTEGER NOT NULL DEFAULT 0,
  status TEXT NOT NULL             -- 'active' | 'sold' | 'passed' | 'cancelled'
)

auction_bids(
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  auction_id INTEGER NOT NULL,
  bidder_id INTEGER NOT NULL,
  amount INTEGER NOT NULL,
  bid_at INTEGER NOT NULL
)

auction_escrow(                    -- 出价实扣灵石托管：spirit_stone 同事务扣入，被超价/流拍退回
  auction_id INTEGER NOT NULL,
  bidder_id INTEGER NOT NULL,
  amount INTEGER NOT NULL,
  PRIMARY KEY(auction_id, bidder_id)
)

-- 结算与查询索引：
CREATE INDEX idx_auctions_status_end ON auctions(status, end_at);   -- 定时结拍扫描
CREATE INDEX idx_auctions_seller ON auctions(seller_id, status);    -- 同时挂拍上限校验
CREATE INDEX idx_auction_bids_auction ON auction_bids(auction_id, bid_at);

-- item_instances 迁移（_ensure_column，幂等）：
item_instances += status TEXT NOT NULL DEFAULT 'normal'   -- 'normal' | 'auction'
-- status='auction' 的实例在装备/强化/重铸/分解/赠送/再次上拍全部路径排除（§6.3）。

-- characters 迁移（_ensure_column，幂等）：
characters += big_fail_streak INTEGER NOT NULL DEFAULT 0
--   字段边界（profile）：仅在【化神→炼虚】突破失败时累加、仅该突破读取加成（+10%/次），
--   该突破成功时清零；低境界大突破不读不写（v2 化神段平衡不受影响）。名字取通用名是为
--   将来推广预留，推广前边界以本注释 + 单测为准。
characters += last_seclusion_start INTEGER   -- 最近一次已结束闭关的起止（§5.2 双修重叠查询用，
characters += last_seclusion_end INTEGER     --   仅保留最近一段，不做全量历史）
characters += natal_instance_id INTEGER      -- 本命法宝认主实例（M5，§7.2）

-- item_instances 迁移（M5）：
item_instances += natal_level INTEGER NOT NULL DEFAULT 0  -- 本命等级，认主后才会 > 0

-- tribulation_sessions 迁移（M1）：
tribulation_sessions += reward_flag TEXT     -- 心魔劫奖励状态（§7.1），结算时读取

-- 新增 kv 表（若尚无等价物）：
game_flags(key TEXT PRIMARY KEY, value TEXT NOT NULL)   -- 降档宽限截止等运行时旗标
```

所有 schema 改动幂等、旧档无损（`_ensure_column` 或独立可重入迁移），沿用 v2 DoD。

---

## 9. 平衡与验收测试

必须新增/更新的测试：

- 炼虚配置完整：`REALM_NAMES`、`STAMINA_CAP`、`SECLUSION_STAGE_HOURS`、`BIG_BREAKTHROUGH`、`STAMINA_BUY_BASE` 都含 realm 5。
- 化神圆满可尝试炼虚突破，缺炼虚丹返回 `need_pill`；失败轻惩罚不跌境。
- 炼虚门槛离散验收：化神圆满满 buff 档（含满淬炼、道途拉满、飞升被动满）可磨炼虚易图小怪、
  可**低效磨中图普通小怪**（过渡档：单位精力收益显著差于易图但 > 0）；打炼虚中图 Boss / 难图
  挑战成功率 `< 5%`；炼虚初/中/后期分别稳定解锁易（含中图小怪）/中/难图。
- 炼虚推进时长：balance_sim 校验普通活跃档"炼虚初期→圆满"落在 **6~9 周**、高活跃档 ≥4 周（§3.2）。
- **SECLUSION clamp 占用切片**：列出宗门/道途/飞升/据点/双修/传承各来源在典型档位下的占用，
  验证"普通配置档（宗门 + 单道途）"下双修/传承有完整边际收益；满 buff 档下被截断时收功文案
  含"已达闭关增益上限"提示。
- 炼虚经济：地图灵石/精力产出低于首买成本（300）至少 25%；反套利红线回归 `test_economy.py`；
  并**分两档回归**——存量化神档（化神余粮 + 280 精力上限）与新进化神档（无余粮现刷现出）的
  灵石流水、精力负担分别在目标区间内。
- 虚空神殿：炼虚初期推进 40%~70%，炼虚圆满稳定通关。
- 溢出分流：追加 realm 5 后，炼虚圆满走 8% 道行 + 每十万溢出修为凝 1 飞升点，凝点周上限 14；化神圆满未突破在**宽限期内维持
  顶点档、期满自动落入次顶点档**（3% 道行、0 飞升点），宽限截止按 `OVERFLOW_DEMOTE_GRACE_UNTIL`
  单测两侧时间点；`OVERFLOW_DAOHANG_WEEKLY_CAP=600` 与凝点周上限跨档生效，凝点零头跨周保留，
  达到周上限后的完整十万修为不得囤至下周。
- 降档公告为 M0 上线前置验收：版本公告 / `/help` / 收功文案三处含宽限截止与档位说明。
- 炼虚丹首破路径：balance_sim 校验化神圆满正常日程 2~4 周期望获得首枚炼虚丹，且期望来源
  100% 在化神可及内容（不依赖炼虚图掉落）；按 45% + 失败累计保底校验**期望成功突破周期 4~6 周**。
- 突破保底：失败后 `big_fail_streak` 递增、下次基础率 +10%，成功清零，总率仍 ≤ 95%；
  残方 ×4 合成炼虚丹（产物绑定）可用且不可绕过绑定语义。
- 师徒：境界差门槛生效；每对每里程碑终身一次（解除重拜不重发）；传功每日一次、额度受限、
  **徒弟当日无活跃则领取被拒**；出师需元婴 + 累计活跃天数 ≥ 8 双条件（门槛常量与高活跃档到元婴天数对齐）；出师奖励全绑定。
- 社交约束由 DB 兜底：徒弟双拜被唯一索引拒绝；道侣镜像双行成对一致、一人一侣被唯一索引拒绝；
  pending 48h 过期置 `expired`；**declined/expired 不触发冷却**（被拒后可立即发起新关系）；
  `dissolved` 后 7 天冷却可查询且生效。
- 师徒活跃天数落库：徒弟当日首个前台行为使 `active_days +1` 且同日重复行为不重计；
  解除冻结计数、重拜归零；出师校验读取该列。
- 师父周活跃回报：按活跃日计、每徒弟每周封顶、计入道行周上限、出师后停发——各一条单测。
- 共修仪式：每人每周 1 次跨关系去重；邀请 10 分钟超时作废；双方同步开始/一方中途进入其他
  前台活动的处理确定；奖励为独立限周包、不进 SECLUSION clamp。
- 心魔劫：选择"直面心魔"后本段无减伤；成功发 `道心通明`（24h、+10%、进 clamp）+ 道行，
  失败额外扣 5% 修为；`reward_flag` 迁移幂等。
- `last_seclusion_start/end` 在收功时正确写回，且道侣结算窗口内交集计算使用该区间。
- 道侣：结契双向 token；解除费用与冷却生效；双修按重叠闭关时段折算（**含双方离线自动闭关
  的重叠区间**，先按 OFFLINE_CAP 截断后求交）、计入 SECLUSION clamp（回归 `test_buff_caps.py`）、
  **主动收功与自动收功两条路径结果一致**且文案含同参时长与额外修为；互赠白名单外物品被拒。
- 拍卖行：绑定物与禁售名单不可上拍；已装备实例不可上拍；`status='auction'` 实例的装备/强化/
  重铸/分解/赠送全部被拒；出价从 `spirit_stone` 实扣入 escrow、被超价原子退回（并发不超卖）；
  防狙击顺延最多 6 次；**结拍幂等**（重复触发 no-op）；流拍退货退款、成交过户 + 扣税正确并
  恢复实例 `status='normal'`；挂拍费为净 sink。
- buyout：`buyout ≥ 起拍价 ×1.2` 校验；满 buyout 立即成交且不触发顺延；此前最高价者 escrow
  原子退回；出价 ≥ buyout 按 buyout 成交不多收；buyout 成交落 bids 与事件日志。
- 新增百分比加成（传承、双修、道心通明、本命法宝）不突破 §合算上限，clamp 测试全量回归。
- **周事件总量审计（每里程碑上线前必做）**：列出"活跃玩家每周必做事件清单"（Boss、宗门战、
  周活动、飞升试炼、共修、双人任务、周回报…）及预估投入时长；原则：新增周期性必做事件时
  必须同步降级/合并旧事件或明确其为"可错过无惩罚"，**周必做总耗时不随里程碑线性增长**。

---

## 10. 三期里程碑

| 里程碑 | 范围 | 目标 |
|---|---|---|
| **V3-M0 炼虚开放** | v2 收口（#45 道行来源、满淬炼档进 balance_sim）+ 炼虚配置、炼虚丹（含残方合成与失败累计保底）、易/中/难图、化神→炼虚突破（虚空劫）+ M1 线索埋点（图纸残页、炼制"待解锁"条目）+ 降档宽限期旗标与公告文案（上线前置验收） | 化神圆满玩家获得炼虚突破与炼虚地图目标 |
| **V3-M1 炼虚完整链路 + 心魔劫** | 虚空神殿、炼虚 Boss、炼虚装备/材料炼制链、炼虚 GEARED 档回归；心魔劫选项（§7.1） | 炼虚期 PvE、群 Boss 与大突破选项完整 |
| **V3-M2 拍卖行** | 英式拍卖 + buyout、escrow 实扣、防狙击、实例托管、审计、白名单材料、曝光提醒 | 高价值法宝实例与高阶材料支持价格发现 |
| **V3-M3 师徒 + 新角色引导** | 拜师/出师、传功、里程碑奖励、周活跃回报、桃李称号、七日引导任务链、反滥用测试 | 新角色承接与高境界传承收益形成完整链路 |
| **V3-M4 道侣 + 共修仪式** | 结契/解除、双修、互赠、双人周任务、同步共修仪式（覆盖师徒/道侣两种关系）、播报 | 双向关系绑定与限周协作收益完整 |
| **V3-M5 本命法宝雏形** | 认主/解缚、喂养成长、器修协同、clamp 与交易隔离回归（§7.2） | 与境界正交的横向长线，承接炼虚材料 sink |

里程碑依赖顺序：先做 **V3-M0**（其中 v2 收口两项最先）。拍卖行保持独立开发独立验收，
不与炼虚完整链路混做；M2 上线前白名单材料走坊市过渡（§6.1）。M3/M4 可互换，但同步共修仪式
在二者中后上线的里程碑内同时覆盖师徒与道侣两种关系。

---

## 11. 暂不进入 v3.0 的内容

- 合体、大乘等更高境界（四期）。
- 灵兽、傀儡等更重的横向养成轴（四期；心魔劫与本命法宝雏形已进 v3，见 §7）。
- 跨群世界 Boss / 全服战 / 全服天梯 / 跨群拍卖橱窗（拍卖曝光的跨群部分见 §6.5 待评估）。
- 玩家间资源掠夺型生死斗（若未来做：双向确认 + 限额 + 冷却 + 不可夺绑定物 + 日志可查）。
- 任何付费加速、付费突破、付费转修、付费外观。

---
